<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="theme-color" content="#0a0a1a">
<title>TaskFlow Pro + Crypto</title>
<style>
:root {
  --bg-primary: #06060f;
  --bg-secondary: #0d0d1a;
  --bg-tertiary: #141428;
  --bg-card: rgba(20, 20, 50, 0.7);
  --bg-glass: rgba(255,255,255,0.03);
  --accent: #6c5ce7;
  --accent-light: #a29bfe;
  --accent-glow: rgba(108,92,231,0.4);
  --success: #00d68f;
  --success-glow: rgba(0,214,143,0.3);
  --warning: #ffaa00;
  --danger: #ff4757;
  --info: #1e90ff;
  --gold: #ffd700;
  --text-primary: #f0f0f8;
  --text-secondary: #8888a8;
  --text-muted: #55557a;
  --border: rgba(255,255,255,0.06);
  --border-glow: rgba(108,92,231,0.3);
  --radius: 16px;
  --radius-sm: 10px;
  --radius-xs: 6px;
  --sidebar-w: 280px;
  --header-h: 56px;
  --nav-h: 72px;
  --safe-bottom: env(safe-area-inset-bottom, 0px);
}

* { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }

body {
  font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Segoe UI', system-ui, sans-serif;
  background: var(--bg-primary);
  color: var(--text-primary);
  overflow: hidden;
  height: 100dvh;
  overscroll-behavior: none;
}

::-webkit-scrollbar { width:4px; }
::-webkit-scrollbar-track { background:transparent; }
::-webkit-scrollbar-thumb { background:var(--bg-tertiary); border-radius:2px; }

/* ========== LAYOUT ========== */
.app-container { display:flex; height:100dvh; position:relative; }

/* ========== SIDEBAR DESKTOP ========== */
.sidebar {
  width: var(--sidebar-w); background: linear-gradient(180deg, #0d0d20 0%, #0a0a18 100%);
  border-right: 1px solid var(--border); display:flex; flex-direction:column;
  z-index:100; flex-shrink:0; position:relative;
}

.sidebar::before {
  content:''; position:absolute; top:0; right:0; width:1px; height:100%;
  background: linear-gradient(180deg, var(--accent-glow), transparent 60%);
  opacity:0.5;
}

.sidebar-header { padding:20px 18px 16px; border-bottom:1px solid var(--border); }
.sidebar-logo-wrap { display:flex; align-items:center; gap:12px; margin-bottom:4px; }
.sidebar-logo {
  width:42px; height:42px; background:linear-gradient(135deg, #6c5ce7, #a29bfe, #00d68f);
  border-radius:12px; display:flex; align-items:center; justify-content:center;
  font-size:22px; font-weight:900; color:white; position:relative;
  box-shadow: 0 4px 20px rgba(108,92,231,0.3);
}

.sidebar-logo::after {
  content:''; position:absolute; inset:-2px; border-radius:14px;
  background:linear-gradient(135deg, #6c5ce7, #a29bfe, #00d68f);
  z-index:-1; opacity:0.3; filter:blur(8px);
}

.sidebar-brand h1 { font-size:17px; font-weight:800; letter-spacing:-0.3px; }
.sidebar-brand span { font-size:11px; color:var(--text-muted); font-weight:500; }

/* Streak Badge in Sidebar */
.sidebar-streak {
  margin-top:12px; padding:10px 14px; background:linear-gradient(135deg, rgba(255,170,0,0.1), rgba(255,215,0,0.05));
  border:1px solid rgba(255,170,0,0.15); border-radius:var(--radius-sm);
  display:flex; align-items:center; gap:8px;
}

.streak-fire { font-size:24px; animation:pulse-fire 1s infinite alternate; }
@keyframes pulse-fire { from{transform:scale(1);} to{transform:scale(1.15);} }
.streak-num { font-size:22px; font-weight:900; color:var(--gold); }
.streak-label { font-size:10px; color:var(--text-muted); text-transform:uppercase; letter-spacing:0.5px; }

.sidebar-nav { flex:1; padding:10px 8px; overflow-y:auto; }
.nav-section-title { font-size:9px; text-transform:uppercase; letter-spacing:2px; color:var(--text-muted); padding:14px 14px 6px; font-weight:700; }

.nav-item {
  display:flex; align-items:center; gap:12px; padding:10px 14px; border-radius:var(--radius-sm);
  cursor:pointer; transition:all 0.2s; color:var(--text-secondary); font-size:13.5px;
  font-weight:500; margin-bottom:2px; position:relative; user-select:none;
}

.nav-item:hover { background:rgba(255,255,255,0.04); color:var(--text-primary); }
.nav-item.active {
  background:linear-gradient(135deg, rgba(108,92,231,0.2), rgba(108,92,231,0.05));
  color:var(--accent-light); font-weight:600;
}
.nav-item.active::before {
  content:''; position:absolute; left:0; top:50%; transform:translateY(-50%);
  width:3px; height:24px; background:var(--accent); border-radius:0 3px 3px 0;
}

.nav-item .icon { font-size:19px; width:26px; text-align:center; }
.nav-item .nav-badge {
  margin-left:auto; background:var(--danger); color:white; font-size:10px;
  padding:2px 7px; border-radius:8px; font-weight:700; min-width:20px; text-align:center;
}
.nav-item.active .nav-badge { background:rgba(108,92,231,0.4); }

/* ========== MAIN ========== */
.main-content { flex:1; display:flex; flex-direction:column; overflow:hidden; min-width:0; position:relative; }

.header {
  height: var(--header-h); background:rgba(10,10,20,0.8); backdrop-filter:blur(20px);
  border-bottom:1px solid var(--border); display:flex; align-items:center;
  padding:0 16px; gap:12px; flex-shrink:0; position:relative; z-index:50;
}

.menu-toggle { display:none; background:none; border:none; color:var(--text-primary); font-size:22px; cursor:pointer; padding:4px; }

.header-search-wrap { flex:1; max-width:480px; position:relative; }
.header-search-wrap input {
  width:100%; padding:9px 14px 9px 38px; background:var(--bg-tertiary); border:1px solid var(--border);
  border-radius:var(--radius-sm); color:var(--text-primary); font-size:13.5px; outline:none; transition:all 0.2s;
}
.header-search-wrap input:focus { border-color:var(--accent); box-shadow:0 0 0 3px var(--accent-glow); }
.header-search-wrap input::placeholder { color:var(--text-muted); }
.header-search-wrap .search-icon { position:absolute; left:12px; top:50%; transform:translateY(-50%); color:var(--text-muted); font-size:15px; pointer-events:none; }

.header-right { display:flex; align-items:center; gap:6px; margin-left:auto; }
.xp-bar-mini { display:flex; align-items:center; gap:6px; padding:6px 12px; background:linear-gradient(135deg, rgba(108,92,231,0.15), rgba(162,155,254,0.05)); border-radius:20px; border:1px solid rgba(108,92,231,0.2); }
.xp-bar-mini .xp-icon { font-size:14px; }
.xp-bar-mini .xp-text { font-size:12px; font-weight:700; color:var(--accent-light); }
.xp-bar-mini .lvl-text { font-size:10px; color:var(--text-muted); margin-left:2px; }

.avatar-btn {
  width:36px; height:36px; border-radius:50%; background:linear-gradient(135deg, var(--accent), #a29bfe);
  display:flex; align-items:center; justify-content:center; font-size:14px; font-weight:700; cursor:pointer;
}

/* ========== CONTENT AREA ========== */
.content { flex:1; overflow-y:auto; padding:20px; padding-bottom: calc(var(--nav-h) + 20px); }

@media(min-width:769px) {
  .content { padding-bottom:20px; }
}

/* ========== PAGE SECTIONS ========== */
.page-section { display:none; animation:pageIn 0.35s ease; }
.page-section.active { display:block; }

@keyframes pageIn { from{opacity:0;transform:translateY(12px);} to{opacity:1;transform:translateY(0);} }

/* ========== PAGE HEADER ========== */
.page-header { margin-bottom:24px; }
.page-title-row { display:flex; align-items:center; justify-content:space-between; flex-wrap:wrap; gap:12px; }
.page-title h2 { font-size:clamp(22px,3vw,28px); font-weight:800; letter-spacing:-0.5px; background:linear-gradient(135deg,var(--text-primary),var(--accent-light)); -webkit-background-clip:text; -webkit-text-fill-color:transparent; }
.page-title p { font-size:13px; color:var(--text-secondary); margin-top:2px; }

/* ========== CARDS & GLASS ========== */
.glass-card {
  background: var(--bg-glass); backdrop-filter:blur(16px); -webkit-backdrop-filter:blur(16px);
  border:1px solid var(--border); border-radius:var(--radius); padding:20px;
  transition: all 0.3s ease; position:relative; overflow:hidden;
}

.glass-card::before {
  content:''; position:absolute; top:0; left:0; right:0; height:1px;
  background:linear-gradient(90deg, transparent, rgba(255,255,255,0.08), transparent);
}

.glass-card:hover { border-color:rgba(108,92,231,0.2); box-shadow:0 8px 40px rgba(0,0,0,0.3); }

/* ========== STATS GRID ========== */
.stats-grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(160px,1fr)); gap:12px; margin-bottom:24px; }

.stat-card {
  background:var(--bg-glass); backdrop-filter:blur(12px); border:1px solid var(--border);
  border-radius:var(--radius); padding:18px 16px; position:relative; overflow:hidden;
}

.stat-card .sc-icon { font-size:28px; margin-bottom:10px; }
.stat-card .sc-value { font-size:clamp(24px,4vw,32px); font-weight:900; letter-spacing:-1px; }
.stat-card .sc-label { font-size:11.5px; color:var(--text-secondary); margin-top:2px; }

.stat-card.green { border-left:3px solid var(--success); }
.stat-card.purple { border-left:3px solid var(--accent); }
.stat-card.orange { border-left:3px solid var(--warning); }
.stat-card.red { border-left:3px solid var(--danger); }

/* ========== GAMIFICATION - STREAK HEATMAP ========== */
.streak-section { margin-bottom:28px; }
.streak-header { display:flex; align-items:center; justify-content:space-between; margin-bottom:14px; }
.streak-header h3 { font-size:16px; font-weight:700; display:flex; align-items:center; gap:8px; }

.streak-fire-big { font-size:20px; }

.heatmap-grid { display:grid; grid-template-columns:repeat(7,1fr); gap:4px; }
.heatmap-cell {
  aspect-ratio:1; border-radius:6px; background:var(--bg-tertiary);
  display:flex; align-items:center; justify-content:center; font-size:9px; color:var(--text-muted);
  cursor:pointer; transition:all 0.2s; position:relative;
}

.heatmap-cell.l1 { background:rgba(108,92,231,0.15); }
.heatmap-cell.l2 { background:rgba(108,92,231,0.3); }
.heatmap-cell.l3 { background:rgba(108,92,231,0.5); }
.heatmap-cell.l4 { background:rgba(108,92,231,0.75); border-color:var(--accent-glow); }

.heatmap-cell.today { box-shadow:0 0 12px var(--accent-glow); border:1px solid var(--accent); }
.heatmap-cell.future { opacity:0.2; pointer-events:none; }

/* ========== XP PROGRESS RING ========== */
.xp-ring-container { text-align:center; padding:24px; margin-bottom:24px; }
.xp-ring-wrap { position:relative; width:160px; height:160px; margin:0 auto 16px; }
.xp-ring-wrap svg { width:100%; height:100%; transform:rotate(-90deg); }
.xp-ring-bg { fill:none; stroke:var(--bg-tertiary); stroke-width:8; }
.xp-ring-fill { fill:none; stroke:url(#xpGrad); stroke-width:8; stroke-linecap:round; transition:stroke-dashoffset 1s ease; }

.xp-center {
  position:absolute; top:50%; left:50%; transform:translate(-50%,-50%); text-align:center;
}

.xp-center .xp-num { font-size:32px; font-weight:900; background:linear-gradient(135deg,var(--gold),#ffaa00); -webkit-background-clip:text; -webkit-text-fill-color:transparent; }
.xp-center .xp-sub { font-size:10px; color:var(--text-muted); text-transform:uppercase; letter-spacing:1px; }

.level-title { font-size:20px; font-weight:800; margin-bottom:4px; background:linear-gradient(135deg,var(--gold),#ffaa00); -webkit-background-clip:text; -webkit-text-fill-color:transparent; }
.level-desc { font-size:13px; color:var(--text-secondary); margin-bottom:12px; }

.xp-bar-wrap { height:8px; background:var(--bg-tertiary); border-radius:4px; overflow:hidden; margin-top:8px; }
.xp-bar-fill { height:100%; background:linear-gradient(90deg,var(--accent),var(--gold)); border-radius:4px; transition:width 0.8s ease; }

/* ========== DAILY GOALS ========== */
.daily-goals { margin-bottom:28px; }
.daily-goals h3 { font-size:16px; font-weight:700; margin-bottom:14px; display:flex; align-items:center; gap:8px; }

.goals-list { display:flex; flex-direction:column; gap:8px; }
.goal-item {
  display:flex; align-items:center; gap:12px; padding:14px 16px; background:var(--bg-glass);
  border:1px solid var(--border); border-radius:var(--radius-sm); transition:all 0.3s; cursor:pointer;
}

.goal-item.completed { border-color:rgba(0,214,143,0.3); background:rgba(0,214,143,0.05); }

.goal-check {
  width:24px; height:24px; border-radius:50%; border:2px solid var(--text-muted);
  display:flex; align-items:center; justify-content:center; transition:all 0.3s; flex-shrink:0;
}

.goal-item.completed .goal-check { background:var(--success); border-color:var(--success); }
.goal-check.done { animation:popCheck 0.3s ease; }
@keyframes popCheck { 50%{transform:scale(1.4);} }

.goal-check::after { content:'✓'; font-size:14px; color:white; opacity:0; transition:all 0.2s; }
.goal-item.completed .goal-check::after { opacity:1; }

.goal-text { flex:1; font-size:14px; font-weight:500; }
.goal-item.completed .goal-text { text-decoration:line-through; color:var(--text-muted); }

.goal-xp { font-size:12px; color:var(--gold); font-weight:700; }

/* ========== TASK ITEMS - MOBILE OPTIMIZED ========== */
.task-list { display:flex; flex-direction:column; gap:8px; }

.task-item-card {
  display:flex; align-items:center; gap:12px; padding:14px 16px;
  background:var(--bg-glass); border:1px solid var(--border); border-radius:var(--radius-sm);
  transition:all 0.3s; position:relative; overflow:hidden; cursor:pointer; user-select:none;
}

.task-item-card::before {
  content:''; position:absolute; left:0; top:0; bottom:0; width:3px;
  background:var(--priority-color, var(--accent)); border-radius:0 2px 2px 0;
}

.task-item-card.p-urgent { --priority-color:#ff4757; }
.task-item-card.p-high { --priority-color:#ff9f43; }
.task-item-card.p-medium { --priority-color:#ffaa00; }
.task-item-card.p-low { --priority-color:#00d68f; }

.task-item-card:active { transform:scale(0.98); }

.task-check {
  width:22px; height:22px; border-radius:50%; border:2px solid var(--text-muted);
  display:flex; align-items:center; justify-content:center; flex-shrink:0; transition:all 0.3s; cursor:pointer;
}

.task-check.checked { background:var(--success); border-color:var(--success); }
.task-check.checked::after { content:'✓'; font-size:13px; color:white; }

.task-info { flex:1; min-width:0; }
.task-info .task-t { font-size:14px; font-weight:600; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
.task-item-card.completed .task-t { text-decoration:line-through; color:var(--text-muted); }
.task-info .task-meta { font-size:11px; color:var(--text-muted); margin-top:2px; display:flex; gap:8px; align-items:center; flex-wrap:wrap; }

.task-xp { font-size:13px; font-weight:800; color:var(--gold); white-space:nowrap; }
.task-actions { display:flex; gap:4px; }

/* ========== KANBAN - MOBILE ========== */
.kanban-board-mobile { overflow-x:auto; padding-bottom:12px; display:flex; gap:12px; scroll-snap-type:x mandatory; -webkit-overflow-scrolling:touch; }
.kanban-col-mobile { min-width:280px; flex-shrink:0; scroll-snap-align:start; background:var(--bg-glass); border:1px solid var(--border); border-radius:var(--radius); max-height:calc(100dvh - 260px); display:flex; flex-direction:column; }
.kanban-col-h { padding:14px 16px; border-bottom:1px solid var(--border); font-weight:700; font-size:13px; display:flex; align-items:center; gap:8px; position:sticky; top:0; background:rgba(10,10,20,0.95); backdrop-filter:blur(12px); z-index:2; border-radius:var(--radius) var(--radius) 0 0; }
.kanban-col-b { flex:1; overflow-y:auto; padding:10px; }

.kanban-card-m { background:var(--bg-tertiary); border:1px solid var(--border); border-radius:var(--radius-sm); padding:12px; margin-bottom:8px; transition:all 0.2s; touch-action:manipulation; }
.kanban-card-m:active { transform:scale(0.97); }

/* ========== NOTES GRID - MOBILE ========== */
.notes-grid-mobile { display:grid; grid-template-columns:repeat(auto-fill,minmax(160px,1fr)); gap:12px; }
.note-card-m { border-radius:var(--radius); overflow:hidden; position:relative; min-height:140px; padding:16px; cursor:pointer; transition:all 0.3s; display:flex; flex-direction:column; }
.note-card-m:active { transform:scale(0.96); }
.note-card-m .nc-title { font-size:14px; font-weight:700; margin-bottom:8px; }
.note-card-m .nc-preview { font-size:12px; color:rgba(255,255,255,0.6); line-height:1.5; flex:1; display:-webkit-box; -webkit-line-clamp:4; -webkit-box-orient:vertical; overflow:hidden; }
.note-card-m .nc-date { font-size:10px; color:rgba(255,255,255,0.3); margin-top:auto; padding-top:8px; }

/* ========== CHARTS (Canvas) ========== */
.chart-container { position:relative; width:100%; margin-bottom:24px; }
.chart-canvas { width:100%; border-radius:var(--radius-sm); }

/* ========== TRADING MODULE ========== */
.trading-pnl-card {
  background:linear-gradient(135deg, rgba(0,214,143,0.08), rgba(0,214,143,0.02));
  border:1px solid rgba(0,214,143,0.2); border-radius:var(--radius); padding:24px; text-align:center; margin-bottom:20px;
}

.trading-pnl-card.negative { background:linear-gradient(135deg, rgba(255,71,87,0.08), rgba(255,71,87,0.02)); border-color:rgba(255,71,87,0.2); }

.pnl-value { font-size:clamp(32px,6vw,48px); font-weight:900; letter-spacing:-2px; }
.pnl-value.positive { color:var(--success); }
.pnl-value.negative { color:var(--danger); }
.pnl-label { font-size:12px; color:var(--text-secondary); margin-top:4px; text-transform:uppercase; letter-spacing:1px; }

.pnl-row { display:flex; justify-content:center; gap:24px; margin-top:16px; flex-wrap:wrap; }
.pnl-stat { text-align:center; }
.pnl-stat .ps-val { font-size:18px; font-weight:700; }
.pnl-stat .ps-lbl { font-size:10px; color:var(--text-muted); text-transform:uppercase; letter-spacing:0.5px; }

.trading-stats-grid { display:grid; grid-template-columns:repeat(2,1fr); gap:12px; margin-bottom:20px; }
.ts-card { background:var(--bg-glass); border:1px solid var(--border); border-radius:var(--radius-sm); padding:14px; text-align:center; }
.ts-card .ts-val { font-size:20px; font-weight:800; }
.ts-card .ts-lbl { font-size:10px; color:var(--text-muted); margin-top:2px; text-transform:uppercase; letter-spacing:0.5px; }

.news-grid { display:grid; gap:10px; margin-bottom:20px; }
.news-card { background:var(--bg-glass); border:1px solid var(--border); border-radius:var(--radius-sm); padding:14px; transition:all 0.3s; cursor:pointer; }
.news-card:active { transform:scale(0.98); }
.news-card .nc-source { font-size:10px; color:var(--accent-light); text-transform:uppercase; letter-spacing:0.5px; margin-bottom:4px; }
.news-card .nc-title { font-size:13px; font-weight:600; line-height:1.4; margin-bottom:6px; }
.news-card .nc-time { font-size:11px; color:var(--text-muted); }

.trade-journal-list { display:flex; flex-direction:column; gap:8px; }
.trade-item { background:var(--bg-glass); border:1px solid var(--border); border-radius:var(--radius-sm); padding:14px; display:flex; align-items:center; gap:12px; transition:all 0.3s; }
.trade-pnl-badge { font-size:16px; font-weight:800; padding:6px 12px; border-radius:var(--radius-xs); }
.trade-pnl-badge.win { color:var(--success); background:rgba(0,214,143,0.1); }
.trade-pnl-badge.loss { color:var(--danger); background:rgba(255,71,87,0.1); }
.trade-pnl-badge.be { color:var(--warning); background:rgba(255,170,0,0.1); }

.trade-details { flex:1; }
.trade-details .td-pair { font-size:14px; font-weight:700; }
.trade-details .td-meta { font-size:11px; color:var(--text-muted); margin-top:2px; }

/* ========== BADGES / ACHIEVEMENTS ========== */
.badges-grid { display:grid; grid-template-columns:repeat(auto-fill,minmax(140px,1fr)); gap:12px; margin-bottom:28px; }
.badge-card { background:var(--bg-glass); border:1px solid var(--border); border-radius:var(--radius); padding:16px; text-align:center; transition:all 0.3s; }
.badge-card.locked { opacity:0.4; filter:grayscale(1); }
.badge-card.unlocked { border-color:rgba(255,215,0,0.3); box-shadow:0 0 20px rgba(255,215,0,0.1); }
.badge-icon { font-size:36px; margin-bottom:8px; display:block; }
.badge-name { font-size:12px; font-weight:700; margin-bottom:2px; }
.badge-desc { font-size:10px; color:var(--text-muted); }

/* ========== BOTTOM NAV (MOBILE) ========== */
.bottom-nav {
  display:none; position:fixed; bottom:0; left:0; right:0; height:calc(var(--nav-h) + var(--safe-bottom));
  padding-bottom:var(--safe-bottom); background:rgba(10,10,20,0.95); backdrop-filter:blur(20px);
  border-top:1px solid var(--border); z-index:200; justify-content:space-around; align-items:center;
}

.bnav-item { display:flex; flex-direction:column; align-items:center; gap:3px; padding:6px 8px; cursor:pointer; transition:all 0.2s; position:relative; -webkit-tap-highlight-color:transparent; }
.bnav-item.active .bnav-icon { color:var(--accent-light); transform:scale(1.15); }
.bnav-item.active .bnav-label { color:var(--accent-light); }

.bnav-icon { font-size:22px; transition:all 0.3s; color:var(--text-muted); }
.bnav-label { font-size:9px; color:var(--text-muted); font-weight:600; text-transform:uppercase; letter-spacing:0.5px; transition:all 0.2s; }

.bnav-center {
  width:56px; height:56px; border-radius:50%; background:linear-gradient(135deg,var(--accent),#a29bfe);
  display:flex; align-items:center; justify-content:center; font-size:28px; margin-top:-28px;
  box-shadow:0 4px 20px var(--accent-glow); cursor:pointer; transition:all 0.3s; border:3px solid var(--bg-primary);
}

.bnav-center:active { transform:scale(0.9); }

/* ========== FAB / MODAL ========== */
.fab-btn {
  position:fixed; bottom:calc(var(--nav-h) + var(--safe-bottom) + 16px); right:20px; z-index:150;
  width:56px; height:56px; border-radius:50%; background:linear-gradient(135deg,var(--accent),#a29bfe);
  display:flex; align-items:center; justify-content:center; font-size:28px; color:white;
  box-shadow:0 6px 24px var(--accent-glow); cursor:pointer; border:none; transition:all 0.3s;
}

.fab-btn:active { transform:scale(0.9); }

@media(min-width:769px) {
  .fab-btn { bottom:30px; right:calc(var(--sidebar-w) + 30px); }
}

/* ========== MODAL OVERLAY ========== */
.modal-overlay {
  position:fixed; inset:0; background:rgba(0,0,0,0.7); backdrop-filter:blur(8px);
  z-index:500; display:flex; align-items:center; justify-content:center;
  opacity:0; visibility:hidden; transition:all 0.3s; padding:16px;
}

.modal-overlay.active { opacity:1; visibility:visible; }

.modal-box {
  background:linear-gradient(180deg,#141428,#0d0d1a); border:1px solid var(--border);
  border-radius:20px; width:100%; max-width:560px; max-height:90vh; overflow-y:auto;
  transform:translateY(30px) scale(0.95); transition:all 0.3s cubic-bezier(0.4,0,0.2,1);
  box-shadow:0 24px 80px rgba(0,0,0,0.6); position:relative;
}

.modal-overlay.active .modal-box { transform:translateY(0) scale(1); }

.modal-hdr { padding:20px 24px 16px; border-bottom:1px solid var(--border); display:flex; align-items:center; justify-content:space-between; position:sticky; top:0; background:rgba(13,13,26,0.98); backdrop-filter:blur(16px); z-index:5; border-radius:20px 20px 0 0; }
.modal-hdr h3 { font-size:18px; font-weight:800; }
.modal-close { background:none; border:none; color:var(--text-muted); font-size:26px; cursor:pointer; padding:4px; line-height:1; }

.modal-body { padding:20px 24px; }
.modal-ft { padding:16px 24px 24px; border-top:1px solid var(--border); display:flex; justify-content:flex-end; gap:10px; position:sticky; bottom:0; background:rgba(13,13,26,0.98); }

/* ========== FORMS ========== */
.fg { margin-bottom:16px; }
.fg label { display:block; font-size:12px; font-weight:600; color:var(--text-secondary); margin-bottom:6px; text-transform:uppercase; letter-spacing:0.5px; }
.fc {
  width:100%; padding:11px 14px; background:var(--bg-tertiary); border:1px solid var(--border);
  border-radius:var(--radius-sm); color:var(--text-primary); font-size:14px; outline:none; transition:all 0.2s; font-family:inherit;
}

.fc:focus { border-color:var(--accent); box-shadow:0 0 0 3px var(--accent-glow); }
textarea.fc { resize:vertical; min-height:80px; }
select.fc { appearance:none; cursor:pointer; background-image:url("data:image/svg+xml,%3Csvg width='10' height='6' viewBox='0 0 10 6' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 1l4 4 4-4' stroke='%2355557a' stroke-width='1.5'/%3E%3C/svg%3E"); background-repeat:no-repeat; background-position:right 12px center; }

.fr { display:grid; grid-template-columns:1fr 1fr; gap:12px; }
@media(max-width:480px) { .fr { grid-template-columns:1fr; } }

.color-pick { display:flex; gap:8px; flex-wrap:wrap; }
.cp-opt { width:36px; height:36px; border-radius:50%; cursor:pointer; border:3px solid transparent; transition:all 0.2s; }
.cp-opt:hover, .cp-opt.sel { border-color:white; transform:scale(1.1); }

/* ========== BUTTONS ========== */
.btn { padding:10px 20px; border-radius:var(--radius-sm); border:none; cursor:pointer; font-size:14px; font-weight:700; transition:all 0.2s; display:inline-flex; align-items:center; gap:8px; font-family:inherit; user-select:none; }
.btn:active { transform:scale(0.96); }

.btn-primary { background:linear-gradient(135deg,var(--accent),#a29bfe); color:white; box-shadow:0 4px 16px var(--accent-glow); }
.btn-primary:hover { box-shadow:0 6px 24px var(--accent-glow); transform:translateY(-1px); }

.btn-success { background:linear-gradient(135deg,var(--success),#00e6a0); color:white; box-shadow:0 4px 16px var(--success-glow); }
.btn-danger { background:var(--danger); color:white; }
.btn-ghost { background:transparent; color:var(--text-secondary); }
.btn-ghost:hover { background:rgba(255,255,255,0.04); }
.btn-sm { padding:7px 14px; font-size:12px; }

/* ========== CHIPS / FILTERS ========== */
.filter-chips { display:flex; gap:8px; margin-bottom:16px; overflow-x:auto; padding-bottom:4px; -webkit-overflow-scrolling:touch; }
.filter-chip { padding:7px 14px; border-radius:20px; font-size:12.5px; background:var(--bg-glass); border:1px solid var(--border); color:var(--text-secondary); cursor:pointer; transition:all 0.2s; white-space:nowrap; flex-shrink:0; }
.filter-chip:hover { border-color:rgba(108,92,231,0.3); }
.filter-chip.active { background:var(--accent); color:white; border-color:var(--accent); }

/* ========== TOAST ========== */
.toast-wrap { position:fixed; bottom:calc(var(--nav-h) + 80px); right:20px; z-index:600; display:flex; flex-direction:column; gap:8px; }
@media(max-width:768px) { .toast-wrap { bottom:calc(var(--nav-h) + 90px); left:16px; right:16px; } }

.toast { padding:14px 20px; border-radius:var(--radius-sm); display:flex; align-items:center; gap:10px; font-size:13.5px; animation:toastIn 0.3s ease, toastOut 0.3s ease 2.7s forwards; min-width:260px; max-width:400px; }
.toast-success { background:linear-gradient(135deg,rgba(0,214,143,0.15),rgba(0,214,143,0.05)); border:1px solid rgba(0,214,143,0.2); }
.toast-error { background:linear-gradient(135deg,rgba(255,71,87,0.15),rgba(255,71,87,0.05)); border:1px solid rgba(255,71,87,0.2); }
.toast-info { background:linear-gradient(135deg,rgba(108,92,231,0.15),rgba(108,92,231,0.05)); border:1px solid rgba(108,92,231,0.2); }

@keyframes toastIn { from{opacity:0;transform:translateX(40px);} to{opacity:1;transform:translateX(0);} }
@keyframes toastOut { to{opacity:0;transform:translateX(-40px);} }

/* ========== CONFETTI CANVAS ========== */
#confetti-canvas { position:fixed; top:0; left:0; width:100%; height:100%; pointer-events:none; z-index:700; }

/* ========== CALENDAR (mini) ========== */
.mini-cal { display:grid; grid-template-columns:repeat(7,1fr); gap:2px; margin-top:8px; }
.mc-day { aspect-ratio:1; display:flex; align-items:center; justify-content:center; font-size:11px; border-radius:50%; cursor:pointer; transition:all 0.2s; color:var(--text-muted); }
.mc-day.today { background:var(--accent); color:white; font-weight:700; }
.mc-day.active { background:rgba(108,92,231,0.3); color:var(--accent-light); }
.mc-day:hover:not(.today) { background:rgba(255,255,255,0.04); }

/* ========== LEVEL UP ANIMATION ========== */
.level-up-overlay {
  position:fixed; inset:0; z-index:800; display:flex; align-items:center; justify-content:center;
  background:rgba(0,0,0,0.85); backdrop-filter:blur(12px); opacity:0; visibility:hidden; transition:all 0.4s;
}

.level-up-overlay.active { opacity:1; visibility:visible; }
.lup-content { text-align:center; transform:scale(0.5); transition:all 0.5s cubic-bezier(0.34,1.56,0.64,1); }
.level-up-overlay.active .lup-content { transform:scale(1); }
.lup-icon { font-size:80px; animation:pulse-fire 0.5s infinite alternate; margin-bottom:12px; }
.lup-title { font-size:36px; font-weight:900; background:linear-gradient(135deg,var(--gold),#ffaa00); -webkit-background-clip:text; -webkit-text-fill-color:transparent; margin-bottom:8px; }
.lup-sub { font-size:16px; color:var(--text-secondary); }

/* ========== PROGRESS RINGS GRID ========== */
.rings-grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(120px,1fr)); gap:16px; margin-bottom:24px; }
.ring-item { text-align:center; }
.ring-wrap { position:relative; width:90px; height:90px; margin:0 auto 8px; }
.ring-wrap svg { width:100%; height:100%; transform:rotate(-90deg); }
.ring-bg { fill:none; stroke:var(--bg-tertiary); stroke-width:6; }
.ring-fill { fill:none; stroke-width:6; stroke-linecap:round; transition:stroke-dashoffset 1s ease; }
.ring-val { position:absolute; top:50%; left:50%; transform:translate(-50%,-50%); font-size:18px; font-weight:800; }

/* ========== RESPONSIVE / MOBILE ========== */
@media(max-width:768px) {
  .sidebar { position:fixed; top:0; left:-290px; bottom:0; z-index:300; transition:left 0.3s ease; width:280px; }
  .sidebar.open { left:0; box-shadow:10px 0 40px rgba(0,0,0,0.5); }
  .menu-toggle { display:block; }
  .sidebar-overlay { position:fixed; inset:0; background:rgba(0,0,0,0.5); z-index:299; opacity:0; visibility:hidden; transition:all 0.3s; }
  .sidebar-overlay.active { opacity:1; visibility:visible; }
  .bottom-nav { display:flex; }
  .content { padding-bottom:calc(var(--nav-h) + 24px); }
  .fab-btn { bottom:calc(var(--nav-h) + 16px); right:20px; }
  .stats-grid { grid-template-columns:repeat(2,1fr); }
  .notes-grid-mobile { grid-template-columns:repeat(2,1fr); }
  .trading-stats-grid { grid-template-columns:repeat(2,1fr); }
  .projects-grid { grid-template-columns:1fr; }
}

@media(max-width:380px) {
  .notes-grid-mobile { grid-template-columns:1fr; }
  .stats-grid { grid-template-columns:1fr 1fr; gap:8px; }
  .stat-card { padding:14px 12px; }
  .badges-grid { grid-template-columns:repeat(2,1fr); }
}

/* ========== SWIPE INDICATOR ========== */
.swipe-hint { text-align:center; color:var(--text-muted); font-size:12px; margin-bottom:8px; opacity:0.6; }
</style>
</head>
<body>

<div class="app-container">
  <!-- SIDEBAR OVERLAY MOBILE -->
  <div class="sidebar-overlay" id="sidebarOverlay"></div>

  <!-- SIDEBAR -->
  <aside class="sidebar" id="sidebar">
    <div class="sidebar-header">
      <div class="sidebar-logo-wrap">
        <div class="sidebar-logo">TF</div>
        <div class="sidebar-brand">
          <h1>TaskFlow Pro</h1>
          <span>+ Crypto Suite ⚡</span>
        </div>
      </div>
    </div>

    <!-- STREAK -->
    <div style="padding:0 18px 12px;">
      <div class="sidebar-streak">
        <span class="streak-fire">🔥</span>
        <div>
          <div class="streak-num" id="sidebarStreakNum">0</div>
          <div class="streak-label">días de racha</div>
        </div>
      </div>
    </div>

    <!-- NAV -->
    <nav class="sidebar-nav">
      <div class="nav-section-title">Principal</div>
      <div class="nav-item active" data-page="dashboard" onclick="navigateTo('dashboard')">
        <span class="icon">🏠</span> Dashboard<span class="nav-badge" id="navStreakBadge" style="background:var(--gold);color:#000;">0🔥</span>
      </div>
      <div class="nav-item" data-page="tasks" onclick="navigateTo('tasks')">
        <span class="icon">✅</span> Tareas<span class="nav-badge" id="navTaskBadge">0</span>
      </div>
      <div class="nav-item" data-page="projects" onclick="navigateTo('projects')">
        <span class="icon">📁</span> Proyectos
      </div>

      <div class="nav-section-title">Contenido</div>
      <div class="nav-item" data-page="notes" onclick="navigateTo('notes')">
        <span class="icon">📝</span> Notas<span class="nav-badge" id="navNoteBadge">0</span>
      </div>
      <div class="nav-item" data-page="calendar" onclick="navigateTo('calendar')">
        <span class="icon">📅</span> Calendario
      </div>

      <div class="nav-section-title">Crypto ⚡</div>
      <div class="nav-item" data-page="trading" onclick="navigateTo('trading')">
        <span class="icon">💹</span> Trading<span class="nav-badge" style="background:var(--info);">3D</span>
      </div>
      <div class="nav-item" data-page="trading-news" onclick="navigateTo('trading-news')">
        <span class="icon">📰</span> Noticias Crypto
      </div>

      <div class="nav-section-title">Perfil</div>
      <div class="nav-item" data-page="achievements" onclick="navigateTo('achievements')">
        <span class="icon">🏆</span> Logros
      </div>
    </nav>

    <div style="padding:12px 18px;border-top:1px solid var(--border);">
      <div style="font-size:10px;color:var(--text-muted);display:flex;align-items:center;gap:6px;">
        💾 Datos guardados localmente
      </div>
    </div>
  </aside>

  <!-- MAIN -->
  <main class="main-content">
    <!-- HEADER -->
    <header class="header">
      <button class="menu-toggle" onclick="toggleSidebar()">☰</button>
      <div class="header-search-wrap">
        <span class="search-icon">🔍</span>
        <input type="text" placeholder="Buscar..." id="globalSearch" oninput="handleSearch(this.value)">
      </div>
      <div class="header-right">
        <div class="xp-bar-mini">
          <span class="xp-icon">⭐</span>
          <span class="xp-text" id="headerXP">0 XP</span>
          <span class="lvl-text" id="headerLvl">Lv.1</span>
        </div>
        <div class="avatar-btn" onclick="navigateTo('achievements')">U</div>
      </div>
    </header>

    <!-- CONTENT -->
    <div class="content" id="mainContent">

      <!-- ========= DASHBOARD ========= -->
      <section class="page-section active" id="page-dashboard">
        <div class="page-header">
          <div class="page-title-row">
            <div class="page-title">
              <h2 id="dashGreeting">Buenos días ☀️</h2>
              <p id="dashDate"></p>
            </div>
          </div>
        </div>

        <!-- XP RING -->
        <div class="xp-ring-container">
          <div class="xp-ring-wrap">
            <svg viewBox="0 0 160 160">
              <defs>
                <linearGradient id="xpGrad" x1="0%" y1="0%" x2="100%" y2="0%">
                  <stop offset="0%" style="stop-color:#6c5ce7"/>
                  <stop offset="100%" style="stop-color:#ffd700"/>
                </linearGradient>
              </defs>
              <circle class="xp-ring-bg" cx="80" cy="80" r="70"/>
              <circle class="xp-ring-fill" id="xpRingFill" cx="80" cy="80" r="70" stroke-dasharray="439.82" stroke-dashoffset="439.82"/>
            </svg>
            <div class="xp-center">
              <div class="xp-num" id="xpRingNum">1</div>
              <div class="xp-sub">Nivel</div>
            </div>
          </div>
          <div class="level-title" id="levelTitle">Novato</div>
          <div class="level-desc" id="levelDesc">Completa tareas para ganar XP y subir de nivel</div>
          <div style="max-width:280px;margin:12px auto 0;">
            <div style="display:flex;justify-content:space-between;font-size:11px;color:var(--text-muted);margin-bottom:4px;">
              <span id="xpProgressText">0 / 100 XP</span>
              <span>Siguiente nivel</span>
            </div>
            <div class="xp-bar-wrap"><div class="xp-bar-fill" id="xpBarFill" style="width:0%"></div></div>
          </div>
        </div>

        <!-- STREAK HEATMAP -->
        <div class="streak-section">
          <div class="streak-header">
            <h3><span class="streak-fire-big">🔥</span> Tu Racha de Productividad</h3>
            <span style="font-size:13px;color:var(--gold);font-weight:800;" id="streakDisplay">0 días</span>
          </div>
          <div class="heatmap-grid" id="heatmapGrid"></div>
        </div>

        <!-- STATS -->
        <div class="stats-grid" id="dashStats"></div>

        <!-- DAILY GOALS -->
        <div class="daily-goals">
          <h3>🎯 Metas del Día</h3>
          <div class="goals-list" id="dailyGoals"></div>
        </div>

        <!-- TODAY TASKS -->
        <h3 style="margin-bottom:14px;font-size:16px;font-weight:700;">📋 Tareas de Hoy</h3>
        <div class="task-list" id="dashTasks"></div>
      </section>

      <!-- ========= TASKS ========= -->
      <section class="page-section" id="page-tasks">
        <div class="page-header">
          <div class="page-title-row">
            <div class="page-title">
              <h2>✅ Mis Tareas</h2>
              <p>Gestiona y cumple tus objetivos</p>
            </div>
          </div>
          <button class="btn btn-primary" onclick="openTaskModal()">⚡ + Tarea</button>
        </div>

        <div class="filter-chips">
          <span class="filter-chip active" onclick="filterTasks('all',this)">Todas</span>
          <span class="filter-chip" onclick="filterTasks('pending',this)">⏳ Pendientes</span>
          <span class="filter-chip" onclick="filterTasks('in-progress',this)">🔄 En Progreso</span>
          <span class="filter-chip" onclick="filterTasks('completed',this)">✅ Hechas</span>
          <span class="filter-chip" onclick="filterTasks('overdue',this)">🔴 Vencidas</span>
        </div>

        <!-- Chart for tasks -->
        <div class="chart-container"><canvas id="tasksChart" height="180" style="margin-bottom:20px;"></canvas></div>

        <div class="task-list" id="tasksList"></div>
      </section>

      <!-- ========= PROJECTS ========= -->
      <section class="page-section" id="page-projects">
        <div class="page-header">
          <div class="page-title-row">
            <div class="page-title">
              <h2>📁 Proyectos</h2>
              <p>Tus proyectos activos y en pausa</p>
            </div>
            <button class="btn btn-primary" onclick="openProjectModal()">+ Proyecto</button>
          </div>
        </div>
        <div class="filter-chips">
          <span class="filter-chip active" onclick="filterProjects('all',this)">Todos</span>
          <span class="filter-chip" onclick="filterProjects('active',this)">🟢 Activos</span>
          <span class="filter-chip" onclick="filterProjects('completed',this)">✅ Completados</span>
        </div>
        <div class="projects-grid" id="projectsGrid"></div>
      </section>

      <!-- ========= NOTES ========= -->
      <section class="page-section" id="page-notes">
        <div class="page-header">
          <div class="page-title-row">
            <div class="page-title">
              <h2>📝 Mis Notas</h2>
              <p>Captura tus ideas al instante</p>
            </div>
            <button class="btn btn-primary" onclick="openNoteModal()">+ Nota</button>
          </div>
        </div>
        <div class="filter-chips">
          <span class="filter-chip active" onclick="filterNotes('all',this)">Todas</span>
          <span class="filter-chip" onclick="filterNotes('personal',this)">🟣 Personal</span>
          <span class="filter-chip" onclick="filterNotes('work',this)">🔵 Trabajo</span>
          <span class="filter-chip" onclick="filterNotes('ideas',this)">💡 Ideas</span>
        </div>
        <div class="notes-grid-mobile" id="notesGrid"></div>
      </section>

      <!-- ========= CALENDAR ========= -->
      <section class="page-section" id="page-calendar">
        <div class="page-header">
          <div class="page-title-row">
            <div class="page-title">
              <h2>📅 Calendario</h2>
              <p>Tus compromisos organizados</p>
            </div>
            <div style="display:flex;gap:8px;">
              <button class="btn btn-ghost btn-sm" onclick="calNav(-1)">◀</button>
              <span id="calTitle" style="padding:9px 14px;font-weight:700;font-size:14px;"></span>
              <button class="btn btn-ghost btn-sm" onclick="calNav(1)">▶</button>
            </div>
          </div>
        </div>
        <div style="display:grid;grid-template-columns:repeat(7,1fr);gap:3px;" id="calGrid"></div>
      </section>

      <!-- ========= TRADING ========= -->
      <section class="page-section" id="page-trading">
        <div class="page-header">
          <div class="page-title-row">
            <div class="page-title">
              <h2>💹 Trading Desk</h2>
              <p>Tu centro de operaciones crypto</p>
            </div>
            <button class="btn btn-success" onclick="openTradeModal()">+ Nuevo Trade</button>
          </div>
        </div>

        <!-- P&L Card -->
        <div class="trading-pnl-card" id="pnlCard">
          <div class="pnl-label">Profit & Loss de Hoy</div>
          <div class="pnl-value positive" id="todayPnl">$0.00</div>
          <div class="pnl-row">
            <div class="pnl-stat"><div class="ps-val" id="totalTrades">0</div><div class="ps-lbl">Total Trades</div></div>
            <div class="pnl-stat"><div class="ps-val" style="color:var(--success);" id="winRate">0%</div><div class="ps-lbl">Win Rate</div></div>
            <div class="pnl-stat"><div class="ps-val" id="avgRR">0:1</div><div class="ps-lbl">Avg R:R</div></div>
          </div>
        </div>

        <!-- Trading Stats -->
        <div class="trading-stats-grid">
          <div class="ts-card"><div class="ts-val" style="color:var(--success);" id="totalWins">0</div><div class="ts-lbl">Victorias</div></div>
          <div class="ts-card"><div class="ts-val" style="color:var(--danger);" id="totalLosses">0</div><div class="ts-lbl">Derrotas</div></div>
          <div class="ts-card"><div class="ts-val" style="color:var(--gold);" id="bestTrade">$0</div><div class="ts-lbl">Mejor Trade</div></div>
          <div class="ts-card"><div class="ts-val" style="color:var(--warning);" id="consecWins">0</div><div class="ts-lbl">Racha Ganadora</div></div>
        </div>

        <!-- P&L Chart -->
        <div class="chart-container"><canvas id="pnlChart" height="200"></canvas></div>

        <!-- Weekly Overview -->
        <h3 style="margin-bottom:12px;font-size:15px;font-weight:700;">📊 Resumen Semanal</h3>
        <div class="chart-container"><canvas id="weeklyChart" height="160"></canvas></div>

        <!-- Trade Journal -->
        <h3 style="margin-bottom:12px;font-size:15px;font-weight:700;">📓 Libro de Trades</h3>
        <div class="trade-journal-list" id="tradeJournal"></div>
      </section>

      <!-- ========= TRADING NEWS ========= -->
      <section class="page-section" id="page-trading-news">
        <div class="page-header">
          <div class="page-title-row">
            <div class="page-title">
              <h2>📰 Noticias Crypto</h2>
              <p>Resumen diario del mercado cripto</p>
            </div>
            <button class="btn btn-ghost btn-sm" onclick="loadCryptoNews()">🔄 Actualizar</button>
          </div>
        </div>

        <!-- Market Overview -->
        <div class="trading-stats-grid" id="marketOverview"></div>

        <h3 style="margin:20px 0 12px;font-size:15px;font-weight:700;">📰 Noticias del Día</h3>
        <div class="news-grid" id="newsGrid"></div>
      </section>

      <!-- ========= ACHIEVEMENTS ========= -->
      <section class="page-section" id="page-achievements">
        <div class="page-header">
          <div class="page-title-row">
            <div class="page-title">
              <h2>🏆 Logros & Perfil</h2>
              <p>Tu progreso y medallas ganadas</p>
            </div>
          </div>
        </div>

        <!-- Profile Card -->
        <div class="glass-card" style="margin-bottom:24px;text-align:center;padding:30px;">
          <div style="width:80px;height:80px;border-radius:50%;background:linear-gradient(135deg,var(--accent),var(--gold));margin:0 auto 16px;display:flex;align-items:center;justify-content:center;font-size:36px;font-weight:900;">U</div>
          <h2 style="font-size:22px;font-weight:800;margin-bottom:4px;">Usuario Pro</h2>
          <div style="color:var(--text-muted);margin-bottom:12px;">Nivel <span id="profileLvl" style="color:var(--gold);font-weight:800;">1</span> • <span id="profileXP">0</span> XP total</div>
          <div class="stats-grid" style="max-width:300px;margin:0 auto;">
            <div><div style="font-size:24px;font-weight:900;color:var(--success);" id="totalCompleted">0</div><div style="font-size:10px;color:var(--text-muted);">Completadas</div></div>
            <div><div style="font-size:24px;font-weight:900;color:var(--gold);" id="profileStreak">0</div><div style="font-size:10px;color:var(--text-muted);">Días Racha 🔥</div></div>
            <div><div style="font-size:24px;font-weight:900;color:var(--info);" id="tradesCount">0</div><div style="font-size:10px;color:var(--text-muted);">Trades</div></div>
          </div>
        </div>

        <h3 style="margin-bottom:12px;font-size:15px;font-weight:700;">🎖️ Medallas</h3>
        <div class="badges-grid" id="badgesGrid"></div>
      </section>

    </div><!-- content -->
  </main>
</div>

<!-- BOTTOM NAV (MOBILE) -->
<nav class="bottom-nav" id="bottomNav">
  <div class="bnav-item active" data-page="dashboard" onclick="navigateTo('dashboard')">
    <span class="bnav-icon">🏠</span><span class="bnav-label">Home</span>
  </div>
  <div class="bnav-item" data-page="tasks" onclick="navigateTo('tasks')">
    <span class="bnav-icon">✅</span><span class="bnav-label">Tareas</span>
  </div>
  <div class="bnav-center" onclick="openQuickAdd()">⚡</div>
  <div class="bnav-item" data-page="trading" onclick="navigateTo('trading')">
    <span class="bnav-icon">💹</span><span class="bnav-label">Trading</span>
  </div>
  <div class="bnav-item" data-page="achievements" onclick="navigateTo('achievements')">
    <span class="bnav-icon">🏆</span><span class="bnav-label">Perfil</span>
  </div>
</nav>

<!-- FAB (DESKTOP) -->
<button class="fab-btn" id="fabBtn" onclick="openQuickAdd()" style="display:none;">⚡</button>

<!-- QUICK ADD MODAL -->
<div class="modal-overlay" id="quickModal">
  <div class="modal-box">
    <div class="modal-hdr"><h3>⚡ Captura Rápida</h3><button class="modal-close" onclick="closeMod('quickModal')">×</button></div>
    <div class="modal-body">
      <div class="fg"><label>¿Qué tienes en mente?</label><input type="text" class="fc" id="qaText" placeholder="Escribe aquí..."></div>
      <div class="fr">
        <div class="fg"><label>Tipo</label><select class="fc" id="qaType"><option value="task">✅ Tarea</option><option value="note">📝 Nota</option></select></div>
        <div class="fg"><label>Prioridad</label><select class="fc" id="qaPriority"><option value="low">Baja</option><option value="medium">Media</option><option value="high">Alta</option><option value="urgent">Urgente</option></select></div>
      </div>
      <div class="fr">
        <div class="fg"><label>Proyecto</label><select class="fc" id="qaProject"></select></div>
        <div class="fg"><label>Vence</label><input type="date" class="fc" id="qaDate"></div>
      </div>
    </div>
    <div class="modal-ft"><button class="btn btn-ghost" onclick="closeMod('quickModal')">Cancelar</button><button class="btn btn-primary" onclick="submitQuick()">⚡ Capturar</button></div>
  </div>
</div>

<!-- TASK MODAL -->
<div class="modal-overlay" id="taskModal">
  <div class="modal-box">
    <div class="modal-hdr"><h3 id="taskModalTitle">Nueva Tarea</h3><button class="modal-close" onclick="closeMod('taskModal')">×</button></div>
    <div class="modal-body">
      <input type="hidden" id="tId"><div class="fg"><label>Título *</label><input type="text" class="fc" id="tTitle" placeholder="Tarea..."></div>
      <div class="fg"><label>Descripción</label><textarea class="fc" id="tDesc" rows="2"></textarea></div>
      <div class="fr">
        <div class="fg"><label>Prioridad</label><select class="fc" id="tPrio"><option value="low">Baja</option><option value="medium">Media</option><option value="high">Alta</option><option value="urgent">Urgente</option></select></div>
        <div class="fg"><label>Estado</label><select class="fc" id="tStatus"><option value="pending">Pendiente</option><option value="in-progress">En Progreso</option><option value="completed">Completada</option></select></div>
      </div>
      <div class="fr">
        <div class="fg"><label>Inicio</label><input type="date" class="fc" id="tStart"></div>
        <div class="fg"><label>Vence</label><input type="date" class="fc" id="tDue"></div>
      </div>
      <div class="fr">
        <div class="fg"><label>Proyecto</label><select class="fc" id="tProj"></select></div>
        <div class="fg"><label>Categoría</label><select class="fc" id="tCat"><option value="">Sin categoría</option><option value="dev">💻 Dev</option><option value="design">🎨 Diseño</option><option value="marketing">📢 Marketing</option><option value="research">🔬 Research</option><option value="admin">⚙️ Admin</option><option value="personal">🏠 Personal</option></select></div>
      </div>
    </div>
    <div class="modal-ft"><button class="btn btn-danger btn-sm" id="delTaskBtn" onclick="deleteTask(document.getElementById('tId').value)" style="margin-right:auto;display:none;">🗑️</button><button class="btn btn-ghost" onclick="closeMod('taskModal')">Cancelar</button><button class="btn btn-primary" onclick="saveTask()">💾 Guardar</button></div>
  </div>
</div>

<!-- NOTE MODAL -->
<div class="modal-overlay" id="noteModal">
  <div class="modal-box">
    <div class="modal-hdr"><h3 id="noteModalTitle">Nueva Nota</h3><button class="modal-close" onclick="closeMod('noteModal')">×</button></div>
    <div class="modal-body">
      <input type="hidden" id="nId"><div class="fg"><label>Título</label><input type="text" class="fc" id="nTitle" placeholder="Título..."></div>
      <div class="fr">
        <div class="fg"><label>Categoría</label><select class="fc" id="nCat"><option value="personal">Personal</option><option value="work">Trabajo</option><option value="ideas">Ideas</option></select></div>
        <div class="fg"><label>Color</label><div class="color-pick"><div class="cp-opt sel" style="background:#6c5ce7" data-c="#6c5ce7"></div><div class="cp-opt" style="background:#00b894" data-c="#00b894"></div><div class="cp-opt" style="background:#e17055" data-c="#e17055"></div><div class="cp-opt" style="background:#fdcb6e" data-c="#fdcb6e"></div><div class="cp-opt" style="background:#74b9ff" data-c="#74b9ff"></div><div class="cp-opt" style="background:#fd79a8" data-c="#fd79a8"></div></div></div>
      </div>
      <div class="fg"><label>Contenido</label><textarea class="fc" id="nContent" rows="8" placeholder="Tu nota..."></textarea></div>
    </div>
    <div class="modal-ft"><button class="btn btn-danger btn-sm" id="delNoteBtn" onclick="deleteNote(document.getElementById('nId').value)" style="margin-right:auto;display:none;">🗑️</button><button class="btn btn-ghost" onclick="closeMod('noteModal')">Cancelar</button><button class="btn btn-primary" onclick="saveNote()">💾 Guardar</button></div>
  </div>
</div>

<!-- PROJECT MODAL -->
<div class="modal-overlay" id="projModal">
  <div class="modal-box">
    <div class="modal-hdr"><h3 id="projModalTitle">Nuevo Proyecto</h3><button class="modal-close" onclick="closeMod('projModal')">×</button></div>
    <div class="modal-body">
      <input type="hidden" id="pId"><div class="fg"><label>Nombre *</label><input type="text" class="fc" id="pName" placeholder="Proyecto..."></div>
      <div class="fg"><label>Descripción</label><textarea class="fc" id="pDesc" rows="2"></textarea></div>
      <div class="fr">
        <div class="fg"><label>Estado</label><select class="fc" id="pStatus"><option value="active">Activo</option><option value="paused">Pausado</option><option value="completed">Completado</option></select></div>
        <div class="fg"><label>Prioridad</label><select class="fc" id="pPrio"><option value="low">Baja</option><option value="medium">Media</option><option value="high">Alta</option><option value="urgent">Urgente</option></select></div>
      </div>
      <div class="fr">
        <div class="fg"><label>Inicio</label><input type="date" class="fc" id="pStart"></div>
        <div class="fg"><label>Vence</label><input type="date" class="fc" id="pEnd"></div>
      </div>
      <div class="fg"><label>Color</label><div class="color-pick"><div class="cp-opt sel" style="background:#6c5ce7" data-c="#6c5ce7"></div><div class="cp-opt" style="background:#00b894" data-c="#00b894"></div><div class="cp-opt" style="background:#e17055" data-c="#e17055"></div><div class="cp-opt" style="background:#fdcb6e" data-c="#fdcb6e"></div><div class="cp-opt" style="background:#74b9ff" data-c="#74b9ff"></div></div></div>
    </div>
    <div class="modal-ft"><button class="btn btn-ghost" onclick="closeMod('projModal')">Cancelar</button><button class="btn btn-primary" onclick="saveProject()">💾 Guardar</button></div>
  </div>
</div>

<!-- TRADE MODAL -->
<div class="modal-overlay" id="tradeModal">
  <div class="modal-box">
    <div class="modal-hdr"><h3>📊 Registrar Trade</h3><button class="modal-close" onclick="closeMod('tradeModal')">×</button></div>
    <div class="modal-body">
      <input type="hidden" id="trId"><div class="fr">
        <div class="fg"><label>Par / Activo</label><input type="text" class="fc" id="trPair" placeholder="BTC/USDT"></div>
        <div class="fg"><label>Tipo</label><select class="fc" id="trType"><option value="long">Long 🟢</option><option value="short">Short 🔴</option></select></div>
      </div>
      <div class="fr">
        <div class="fg"><label>Precio Entrada</label><input type="number" step="any" class="fc" id="trEntry"></div>
        <div class="fg"><label>Precio Salida</label><input type="number" step="any" class="fc" id="trExit"></div>
      </div>
      <div class="fr">
        <div class="fg"><label>Tamaño (USD)</label><input type="number" class="fc" id="trSize"></div>
        <div class="fg"><label>P&L ($)</label><input type="number" step="any" class="fc" id="trPnl" placeholder="Auto-calculado o manual"></div>
      </div>
      <div class="fr">
        <div class="fg"><label>Stop Loss</label><input type="number" step="any" class="fc" id="trSL"></div>
        <div class="fg"><label>Take Profit</label><input type="number" step="any" class="fc" id="trTP"></div>
      </div>
      <div class="fr">
        <div class="fg"><label>Estrategia</label><input type="text" class="fc" id="trStrat" placeholder="Ej: Breakout"></div>
        <div class="fg"><label>Emoción</label><select class="fc" id="trEmo"><option value="calm">😌 Calma</option><option value="confident">💪 Confianza</option><option value="fomo">😰 FOMO</option><option value="revenge">🔥 Revenge</option><option value="disciplined">🎯 Disciplinado</option></select></div>
      </div>
      <div class="fg"><label>Notas / Lecciones</label><textarea class="fc" id="trNotes" rows="2"></textarea></div>
    </div>
    <div class="modal-ft"><button class="btn btn-danger btn-sm" id="delTradeBtn" onclick="deleteTrade(document.getElementById('trId').value)" style="margin-right:auto;display:none;">🗑️</button><button class="btn btn-ghost" onclick="closeMod('tradeModal')">Cancelar</button><button class="btn btn-success" onclick="saveTrade()">💾 Registrar</button></div>
  </div>
</div>

<!-- LEVEL UP OVERLAY -->
<div class="level-up-overlay" id="levelUpOverlay">
  <div class="lup-content">
    <div class="lup-icon">🎉</div>
    <div class="lup-title" id="lupTitle">¡NIVEL 2!</div>
    <div class="lup-sub" id="lupSub">¡Excelente progreso! Sigue así.</div>
    <button class="btn btn-primary" style="margin-top:20px;" onclick="closeLevelUp()">¡Genial! 🎊</button>
  </div>
</div>

<!-- CONFETTI -->
<canvas id="confetti-canvas"></canvas>

<!-- TOASTS -->
<div class="toast-wrap" id="toastWrap"></div>

<script>
// ========================================
// TASKFLOW PRO + CRYPTO SUITE
// ========================================

const K = 'tfpro_crypto_v3';
let D = loadData();
let curPage = 'dashboard';
let calDate = new Date();
let currentFilter = { tasks:'all', projects:'all', notes:'all' };

// ==================== DATA ====================
function defData(){ return { projects:[],tasks:[],notes:[],trades:[],activities:[],settings:{theme:'dark'},lastActive:Date.now(),xp:0,level:1,totalCompleted:0,completedDates:{},dailyGoals:[],goalCheck:{}}; }
function loadData(){ try{const r=localStorage.getItem(K);return r?{...defData(),...JSON.parse(r)}:defData();}catch(e){return defData();} }
function saveData(){ try{localStorage.setItem(K,JSON.stringify(D));}catch(e){showToast('Error al guardar','error');} }

function genId(){ return Date.now().toString(36)+Math.random().toString(36).substr(2,8); }

function addAct(txt){ D.activities.unshift({txt,t:Date.now()});if(D.activities.length>50)D.activities=D.activities.slice(0,50);saveData(); }

// ==================== NAVIGATION ====================
function navigateTo(p){
  curPage=p;
  document.querySelectorAll('.page-section').forEach(s=>s.classList.remove('active'));
  const el=document.getElementById('page-'+p);if(el)el.classList.add('active');
  document.querySelectorAll('.nav-item,.bnav-item').forEach(n=>n.classList.toggle('active',n.dataset.page===p));
  renderPage(p);closeSidebar();
}

function renderPage(p){
  updateBadges();
  if(p==='dashboard')renderDashboard();
  else if(p==='tasks'){renderTasks();renderTaskChart();}
  else if(p==='projects')renderProjects();
  else if(p==='notes')renderNotes();
  else if(p==='calendar')renderCalendar();
  else if(p==='trading')renderTrading();
  else if(p==='trading-news')renderNews();
  else if(p==='achievements')renderAchievements();
}

function toggleSidebar(){ document.getElementById('sidebar').classList.toggle('open');document.getElementById('sidebarOverlay').classList.toggle('active'); }
document.getElementById('sidebarOverlay').addEventListener('click',toggleSidebar);

// ==================== MODALS ====================
function openMod(id){document.getElementById(id).classList.add('active');}
function closeMod(id){document.getElementById(id).classList.remove('active');}
document.querySelectorAll('.modal-overlay').forEach(m=>{m.addEventListener('click',e=>{if(e.target===m)m.classList.remove('active');});});

// ==================== TOAST ====================
function showToast(msg,type='success'){
  const t=document.createElement('div');t.className=`toast toast-${type}`;
  t.innerHTML=`<span>${type==='success'?'✅':type==='error'?'❌':'ℹ️'}</span><span>${msg}</span>`;
  document.getElementById('toastWrap').appendChild(t);setTimeout(()=>t.remove(),3000);
}

// ==================== XP & LEVELS ====================
const LEVELS=[{xp:0,title:'Novato',desc:'Acabas de comenzar'},
{xp:100,title:'Aprendiz',desc:'Ya tienes momentum'},
{xp:250,title:'Productivo',desc:'¡La productividad es tu aliada!'},
{xp:500,title:'Experto',desc:'Dominas el arte de organizar'},
{xp:1000,title:'Maestro',desc:'Nivel avanzado de productividad'},
{xp:2000,title:'Leyenda',desc:'Eres imparable'}];

function xpForLevel(lvl){return LEVELS[Math.min(lvl,LEVELS.length-1)]?.xp||99999;}
function addXP(amount){
  const prevLvl=D.level;D.xp+=amount;
  let newLvl=1;for(let i=LEVELS.length-1;i>=0;i--){if(D.xp>=LEVELS[i].xp){newLvl=i+1;break;}}
  if(newLvl>D.level){
    D.level=newLvl;showLevelUp();launchConfetti();
  }
  saveData();updateXPUI();
}

function showLevelUp(){
  const lvl=D.level,title=LEVELS[Math.min(lvl,LEVELS.length-1)-1]?.title||'MAX';
  document.getElementById('lupTitle').textContent=`¡NIVEL ${lvl}!`;
  document.getElementById('lupSub').textContent=title+' — ¡'+D.xp+' XP total!';
  document.getElementById('levelUpOverlay').classList.add('active');
}

function closeLevelUp(){document.getElementById('levelUpOverlay').classList.remove('active');}

function updateXPUI(){
  const lvl=D.level,cur=LEVELS[Math.min(lvl-1,LEVELS.length-1)]?.xp||0;
  const nxt=LEVELS[Math.min(lvl,LEVELS.length-1)]?.xp||99999;
  const pct=Math.min((D.xp-cur)/(nxt-cur)*100,100);

  document.getElementById('headerXP').textContent=D.xp+' XP';
  document.getElementById('headerLvl').textContent='Lv.'+lvl;
  document.getElementById('xpRingNum').textContent=lvl;
  document.getElementById('levelTitle').textContent=LEVELS[Math.min(lvl,LEVELS.length-1)]?.title||'MAX';
  document.getElementById('levelDesc').textContent=LEVELS[Math.min(lvl,LEVELS.length-1)]?.desc||'';
  document.getElementById('xpProgressText').textContent=D.xp+' / '+nxt+' XP';
  document.getElementById('xpBarFill').style.width=pct+'%';

  const circ=439.82;document.getElementById('xpRingFill').style.strokeDashoffset=circ-(circ*pct/100);
}

// ==================== STREAKS ====================
function getStreak(){
  const today=new Date().toDateString();const cd=D.completedDates;let streak=0,d=new Date();
  // Check if today has activity, start from yesterday for the streak calculation
  // Actually: check consecutive days with any completion from today backward
  while(true){
    const ds=d.toDateString();
    if(cd[ds]||cd[Math.floor(d.getTime()/86400000)*86400000]){
      streak++;d.setDate(d.getDate()-1);
    } else break;
  }
  return streak;
}

function renderHeatmap(){
  const g=document.getElementById('heatmapGrid');if(!g)return;
  const today=new Date(),cd=D.completedDates||{};
  let html='';for(let i=27;i>=0;i--){
    const d=new Date(today);d.setDate(d.getDate()-i);const ds=d.toDateString();const key=Math.floor(d.getTime()/86400000)*86400000;
    const has=!!cd[key]||!!cd[ds];const isToday=i===0;const isFuture=i<0;
    let cls='heatmap-cell';if(isToday)cls+=' today';if(has){const d2=new Date(today);d2.setDate(d2.getDate()-i);let c=d1=new Date(today).getTime()-d2.getTime();c=Math.min(Math.floor(c/864e5),4);cls+=` l${c+1}`;}
    const dayNames=['D','L','M','X','J','V','S'];const dow=d.getDay();
    html+=`<div class="${cls}" title="${d.toLocaleDateString()}">${isToday?'●':dayNames[dow]}</div>`;
  }
  g.innerHTML=html;
}

function updateStreakUI(){
  const s=getStreak();
  document.getElementById('sidebarStreakNum').textContent=s;
  document.getElementById('streakDisplay').textContent=s+' días';
  document.getElementById('navStreakBadge').textContent=s+'🔥';
  document.getElementById('profileStreak').textContent=s;
}

// ==================== BADGES / ACHIEVEMENTS ====================
const ALL_BADGES=[
{id:'first_task',icon:'✅',name:'Primera Tarea',desc:'Completa tu primera tarea'},
{id:'streak_3',icon:'🔥',name:'Racha x3',desc:'3 días de racha'},
{id:'streak_7',icon:'💪',name:'Racha x7',desc:'1 semana completa'},
{id:'streak_30',icon:'🌋',name:'Racha x30',desc:'¡Un mes entero!'},
{id:'xp_100',icon:'⭐',name:'Centenar',desc:'Gana 100 XP total'},
{id:'xp_500',icon:'🌟',name:'Medio Mil',desc:'Gana 500 XP'},
{id:'xp_1000',icon:'💫',name:'Mil XP',desc:'Gana 1,000 XP'},
{id:'tasks_20',icon:'📋',name:'Veterano',desc:'Completa 20 tareas'},
{id:'tasks_50',icon:'🏅',name:'Super Veteran',desc:'Completa 50 tareas'},
{id:'trades_10',icon:'📊',name:'Trader Novato',desc:'Registra 10 trades'},
{id:'trades_50',icon:'💹',name:'Trader Pro',desc:'Registra 50 trades'},
{id:'winrate_70',icon:'🎯',name:'Sniper Win Rate',desc:'Win rate >70%'},
{id:'note_10',icon:'📝',name:'Escritor',desc:'Crea 10 notas'},
{id:'projects_3',icon:'📁',name:'Organizador',desc:'Crea 3 proyectos'},
{id:'level_5',icon:'🏆',name:'Nivel 5',desc:'Alcanza el nivel 5'}
];

function getUnlockedBadges(){
  const s=getStreak(),b=new Set();
  if(D.totalCompleted>=1)b.add('first_task');
  if(s>=3)b.add('streak_3');if(s>=7)b.add('streak_7');if(s>=30)b.add('streak_30');
  if(D.xp>=100)b.add('xp_100');if(D.xp>=500)b.add('xp_500');if(D.xp>=1000)b.add('xp_1000');
  if(D.totalCompleted>=20)b.add('tasks_20');if(D.totalCompleted>=50)b.add('tasks_50');
  const trades=D.trades||[];
  if(trades.length>=10)b.add('trades_10');if(trades.length>=50)b.add('trades_50');
  if(trades.length>0){const w=trades.filter(t=>t.pnl>0).length;if(w/trades.length>0.7)b.add('winrate_70');}
  const notes=D.notes||[];if(notes.length>=10)b.add('note_10');
  const projs=D.projects||[];if(projs.length>=3)b.add('projects_3');
  if(D.level>=5)b.add('level_5');
  return b;
}

function renderBadges(){
  const unlocked=getUnlockedBadges();
  document.getElementById('badgesGrid').innerHTML=ALL_BADGES.map(b=>{
    const un=unlocked.has(b.id);
    return`<div class="badge-card ${un?'unlocked':'locked'}"><span class="badge-icon">${b.icon}</span><div class="badge-name">${b.name}</div><div class="badge-desc">${b.desc}</div></div>`;
  }).join('');
}

// ==================== CONFETTI ====================
function launchConfetti(){
  const c=document.getElementById('confetti-canvas');c.width=innerWidth;c.height=innerHeight;
  const ctx=c.getContext('2d'),particles=[];const colors=['#6c5ce7','#00d68f','#ffd700','#ff4757','#1e90ff','#fd79a8','#a29bfe','#55efc4'];
  for(let i=0;i<100;i++){particles.push({x:c.width/2,y:c.height/2,vx:(Math.random()-0.5)*16,vy:-Math.random()*16-4,sz:Math.random()*6+3,col:colors[Math.floor(Math.random()*colors.length)],life:1});}
  function anim(){ctx.clearRect(0,0,c.width,c.height);let alive=false;particles.forEach(p=>{if(p.life<=0)return;alive=true;p.x+=p.vx;p.y+=p.vy;p.vy+=0.3;p.life-=0.012;ctx.globalAlpha=p.life;ctx.fillStyle=p.col;ctx.fillRect(p.x,p.y,p.sz,p.sz);});if(alive)requestAnimationFrame(anim);else ctx.clearRect(0,0,c.width,c.height);}
  anim();
}

// ==================== DASHBOARD ====================
function renderDashboard(){
  const h=new Date().getHours();const gr=h<12?'☀️':h<19?'🌤️':'🌙';const gt=h<12?'Buenos días':'Buenas tardes';
  document.getElementById('dashGreeting').textContent=gt+' '+gr;
  document.getElementById('dashDate').textContent=new Date().toLocaleDateString('es-ES',{weekday:'long',year:'numeric',month:'long',day:'numeric'});

  const pending=D.tasks?.filter(t=>t.status!=='completed').length||0;
  const overdue=(D.tasks||[]).filter(t=>{if(t.status==='completed')return false;if(!t.dueDate)return false;const d=new Date(t.dueDate+'T00:00:00');return d<new Date()&&d.toDateString()!==new Date().toDateString();}).length;
  const done=D.tasks?.filter(t=>t.status==='completed').length||0;

  document.getElementById('dashStats').innerHTML=`
    <div class="stat-card purple"><div class="sc-icon">📋</div><div class="sc-value">${(D.tasks||[]).length}</div><div class="sc-label">Total Tareas</div></div>
    <div class="stat-card orange"><div class="sc-icon">⏳</div><div class="sc-value">${pending}</div><div class="sc-label">Pendientes</div></div>
    <div class="stat-card green"><div class="sc-icon">✅</div><div class="sc-value">${done}</div><div class="sc-label">Completadas</div></div>
    <div class="stat-card red"><div class="sc-icon">🔥</div><div class="sc-value" style="color:var(--gold)">${getStreak()}</div><div class="sc-label">Racha Actual</div></div>`;

  // Daily Goals
  const goals=D.dailyGoals||[];
  document.getElementById('dailyGoals').innerHTML=goals.length?goals.map((g,i)=>{
    const checked=!!D.goalCheck?.[i];return`<div class="goal-item ${checked?'completed':''}" onclick="toggleGoal(${i})"><div class="goal-check ${checked?'done':' '}"></div><span class="goal-text">${escape(g)}</span><span class="goal-xp">+10 XP</span></div>`;
  }).join(''):'<div style="text-align:center;padding:20px;color:var(--text-muted);font-size:13px;">Agrega metas diarias para mantener tu racha 🔥</div>';

  // Dashboard tasks (pending)
  const prio={urgent:0,high:1,medium:2,low:3};
  const pend=(D.tasks||[]).filter(t=>t.status!=='completed').sort((a,b)=>(prio[a.priority]||2)-(prio[b.priority]||2)).slice(0,8);
  document.getElementById('dashTasks').innerHTML=pend.length?pend.map(t=>`
    <div class="task-item-card p-${t.priority} ${t.status==='completed'?'completed':''}" onclick="openTaskModal('${t.id}')">
      <div class="task-check ${t.status==='completed'?'checked':''}" onclick="event.stopPropagation();toggleTaskStatus('${t.id}',this)"></div>
      <div class="task-info"><div class="task-t">${escape(t.title)}</div><div class="task-meta"><span>${getProjName(t.projectId)||''}</span>${t.dueDate?`<span>📅 ${fmtD(t.dueDate)}</span>`:''}</div></div>
      <div class="task-xp">+${xpForTask(t.priority)}XP</div>
    </div>`).join(''):'<div style="text-align:center;padding:30px;color:var(--text-muted);font-size:14px;">No hay tareas pendientes. ¡Felicidades! 🎉</div>';

  renderHeatmap();updateStreakUI();updateXPUI();
}

function toggleGoal(i){
  if(!D.goalCheck)D.goalCheck={};
  const was=!D.goalCheck[i];D.goalCheck[i]=was;
  if(was){addXP(10);D.totalCompleted++;addAct('🎯 Meta diaria completada: <strong>Meta '+(i+1)+'</strong>');showToast('+10 XP','success');}
  saveData();renderDashboard();
}

// ==================== TASKS ====================
function renderTasks(){
  let tasks=[...D.tasks||[]];const f=currentFilter.tasks;
  if(f==='pending')tasks=tasks.filter(t=>t.status!=='completed');
  else if(f==='in-progress')tasks=tasks.filter(t=>t.status==='in-progress');
  else if(f==='completed')tasks=tasks.filter(t=>t.status==='completed');
  else if(f==='overdue'){const n=new Date();tasks=tasks.filter(t=>t.status!=='completed'&&t.dueDate&&new Date(t.dueDate+'T00:00:00')<n);}

  const prio={urgent:0,high:1,medium:2,low:3};
  tasks.sort((a,b)=>(prio[a.priority]||2)-(prio[b.priority]||2));

  document.getElementById('tasksList').innerHTML=tasks.length?tasks.map(t=>`
    <div class="task-item-card p-${t.priority} ${t.status==='completed'?'completed':''}" onclick="openTaskModal('${t.id}')">
      <div class="task-check ${t.status==='completed'?'checked':''}" onclick="event.stopPropagation();toggleTaskStatus('${t.id}',this)"></div>
      <div class="task-info"><div class="task-t">${escape(t.title)}</div><div class="task-meta"><span class="tag tag-${prioTag(t.priority)}" style="font-size:10px;padding:2px 6px;">${pLabel(t.priority)}</span>${getProjName(t.projectId)?'<span>'+getProjName(t.projectId)+'</span>':''}</div></div>
      <div class="task-xp">+${xpForTask(t.priority)}XP</div>
    </div>`).join(''):'<div style="text-align:center;padding:40px;color:var(--text-muted);font-size:15px;">No hay tareas para esta categoría ✨</div>';

  renderTaskChart();populateSelects();
}

function filterTasks(f,el){currentFilter.tasks=f;document.querySelectorAll('#page-tasks .filter-chip').forEach(c=>c.classList.remove('active'));if(el)el.classList.add('active');renderTasks();}

function toggleTaskStatus(id,el){
  const t=D.tasks?.find(x=>x.id===id);if(!t)return;
  t.status=t.status==='completed'?'pending':'completed';
  if(t.status==='completed'){
    addXP(xpForTask(t.priority));D.totalCompleted++;addAct('✅ <strong>"'+t.title+'"</strong> completada (+ '+xpForTask(t.priority)+' XP)');showToast('+ '+xpForTask(t.priority)+' XP','success');checkBadges();
  }else{addAct('⏳ <strong>"'+t.title+'"</strong> marcada pendiente');}

  // Record date for streak
  const key=Math.floor(Date.now()/86400000)*86400000;if(!D.completedDates)D.completedDates={};D.completedDates[key]=true;
  saveData();renderPage(curPage);updateStreakUI();
}

function xpForTask(p){return{urgent:25,high:20,medium:15,low:10}[p]||10;}

function renderTaskChart(){
  const c=document.getElementById('tasksChart');if(!c)return;const ctx=c.getContext('2d');
  const W=c.width=c.offsetWidth*2,H=c.height=360;ctx.scale(1,1);
  // Group by last 7 days
  const days=[];for(let i=6;i>=0;i--){const d=new Date();d.setDate(d.getDate()-i);days.push({d,t:d.toDateString(),c:0});}
  (D.tasks||[]).forEach(t=>{if(t.status==='completed'&&t.updatedAt){const td=new Date(t.updatedAt).toDateString();days.forEach(ds=>{if(ds.t===td)ds.c++;});}});

  const max=Math.max(...days.map(d=>d.c),1);
  ctx.clearRect(0,0,W,H);const pad={t:20,r:20,b:40,l:50};const cw=W-pad.l-pad.r;const ch=H-pad.t-pad.b;

  // Grid
  ctx.strokeStyle='rgba(255,255,255,0.04)';ctx.lineWidth=1;
  for(let i=0;i<=4;i++){const y=pad.t+ch*(i/4);ctx.beginPath();ctx.moveTo(pad.l,y);ctx.lineTo(W-pad.r,y);ctx.stroke();}

  // Bars
  const bw=cw/days.length*0.6;const gap=cw/days.length*0.4;
  days.forEach((d,i)=>{
    const x=pad.l+i*(cw/days.length)+gap/2;const bh=(d.c/max)*ch;const y=pad.t+ch-bh;
    const grad=ctx.createLinearGradient(x,y,x,pad.t+ch);grad.addColorStop(0,'#6c5ce7');grad.addColorStop(1,'rgba(108,92,231,0.3)');
    ctx.fillStyle=grad;roundRect(ctx,x,y,bw,bh,6);ctx.fill();
    // Value on top
    if(d.c>0){ctx.fillStyle='#a29bfe';ctx.font='bold 24px system-ui';ctx.textAlign='center';ctx.fillText(d.c,x+bw/2,y-10);}
    // Label
    ctx.fillStyle='#55557a';ctx.font='18px system-ui';ctx.textAlign='center';
    const dayN=['D','L','M','X','J','V','S'];ctx.fillText(dayN[d.d.getDay()],x+bw/2,H-pad.b+30);
  });
}

function roundRect(ctx,x,y,w,h,r){if(h<1)return;ctx.beginPath();ctx.moveTo(x+r,y);ctx.lineTo(x+w-r,y);ctx.quadraticCurveTo(x+w,y,x+w,y+r);ctx.lineTo(x+w,y+h);ctx.lineTo(x,y+h);ctx.lineTo(x,y+r);ctx.quadraticCurveTo(x,y,x+r,y);ctx.closePath();}

// ==================== PROJECTS ====================
function renderProjects(f){
  let projs=[...D.projects||[]];if(f==='active')projs=projs.filter(p=>p.status==='active');else if(f==='completed')projs=projs.filter(p=>p.status==='completed');
  const g=document.getElementById('projectsGrid');
  g.innerHTML=projs.length?projs.map(p=>{const tasks=(D.tasks||[]).filter(t=>t.projectId===p.id);const done=tasks.filter(t=>t.status==='completed').length;const pct=tasks.length?Math.round(done/tasks.length*100):0;
  return`<div class="glass-card" onclick="openProjectModal('${p.id}')" style="cursor:pointer;margin-bottom:12px;">
    <div style="height:4px;background:${p.color||'var(--accent)'};border-radius:2px;margin-bottom:14px;"></div>
    <h3 style="font-size:16px;font-weight:700;margin-bottom:4px;">${escape(p.name)}</h3>
    <span class="tag tag-${p.status==='active'?'green':p.status==='completed'?'blue':'gray'}" style="font-size:11px;padding:3px 8px;">${sLabel(p.status)}</span>
    ${p.description?`<div style="color:var(--text-secondary);font-size:12.5px;margin:8px 0;line-height:1.5;">${escape(p.description).substring(0,120)}</div>`:''}
    <div style="display:flex;align-items:center;gap:8px;margin-top:12px;font-size:12px;color:var(--text-muted);">
      <span>${done}/${tasks.length} tareas</span><div style="flex:1;height:5px;background:var(--bg-tertiary);border-radius:3px;"><div style="height:100%;width:${pct}%;background:${p.color||'var(--accent)'};border-radius:3px;transition:width 0.5s;"></div></div><span>${pct}%</span>
    </div></div>`}).join(''):'<div style="text-align:center;padding:40px;color:var(--text-muted);font-size:15px;">No hay proyectos 📁</div>';
}

function filterProjects(f,el){currentFilter.projects=f;document.querySelectorAll('#page-projects .filter-chip').forEach(c=>c.classList.remove('active'));if(el)el.classList.add('active');renderProjects(f);}

function openProjectModal(id){
  document.getElementById('pId').value=id||'';document.getElementById('pName').value='';document.getElementById('pDesc').value='';
  document.getElementById('pStatus').value='active';document.getElementById('pPrio').value='medium';
  document.getElementById('pStart').value='';document.getElementById('pEnd').value='';
  document.querySelectorAll('#projModal .cp-opt').forEach(c=>c.classList.toggle('selected',c===document.querySelector('#projModal .cp-opt')));
  if(id){const p=D.projects?.find(x=>x.id===id);if(p){document.getElementById('projModalTitle').textContent='Editar Proyecto';document.getElementById('pId').value=p.id;document.getElementById('pName').value=p.name;document.getElementById('pDesc').value=p.description||'';document.getElementById('pStatus').value=p.status;document.getElementById('pPrio').value=p.priority;document.getElementById('pStart').value=p.startDate||'';document.getElementById('pEnd').value=p.endDate||'';document.querySelectorAll('#projModal .cp-opt').forEach(c=>c.classList.toggle('selected',c.dataset.c===p.color));}}else{document.getElementById('projModalTitle').textContent='Nuevo Proyecto';}populateSelects();openMod('projModal');
}

function saveProject(){
  const name=document.getElementById('pName').value.trim();if(!name){showToast('Nombre requerido','error');return;}
  const id=document.getElementById('pId').value;const sel=document.querySelector('#projModal .cp-opt.selected');
  const data={name,description:document.getElementById('pDesc').value.trim(),status:document.getElementById('pStatus').value,priority:document.getElementById('pPrio').value,startDate:document.getElementById('pStart').value,endDate:document.getElementById('pEnd').value,color:sel?sel.dataset.c:'#6c5ce7'};
  if(id){const i=D.projects?.findIndex(x=>x.id===id);if(i>=0)D.projects[i]={...D.projects[i],...data};addAct('📁 Editado: <strong>'+name+'</strong>');showToast('Proyecto actualizado');}else{data.id=genId();data.createdAt=Date.now();(D.projects||(D.projects=[])).push(data);addAct('📁 Creado: <strong>'+name+'</strong>');showToast('Proyecto creado');}saveData();closeMod('projModal');renderPage(curPage);}

// ==================== NOTES ====================
function renderNotes(f){
  let notes=[...D.notes||[]];if(f!=='all')notes=notes.filter(n=>n.category===f);
  notes.sort((a,b)=>(b.updatedAt||b.createdAt||0)-(a.updatedAt||a.createdAt||0));
  const g=document.getElementById('notesGrid');
  g.innerHTML=notes.length?notes.map(n=>`<div class="note-card-m glass-card" onclick="openNoteModal('${n.id}')" style="background:linear-gradient(135deg,${n.color}15,${n.color}08);border-color:${n.color}30;">
    <div style="font-size:12px;margin-bottom:6px;">📌</div><div class="nc-title">${escape(n.title||'Sin título')}</div><div class="nc-preview">${escape((n.content||'').substring(0,120))}</div>
    <div class="nc-date">${fmtD(n.updatedAt||n.createdAt)}</div></div>`).join(''):'<div style="text-align:center;padding:40px;color:var(--text-muted);font-size:15px;grid-column:1/-1;">No hay notas 📝</div>';
  document.getElementById('navNoteBadge').textContent=D.notes?.length||0;
}

function filterNotes(f,el){currentFilter.notes=f;document.querySelectorAll('#page-notes .filter-chip').forEach(c=>c.classList.remove('active'));if(el)el.classList.add('active');renderNotes(f);}

function openNoteModal(id){
  document.getElementById('nId').value=id||'';document.getElementById('nTitle').value='';document.getElementById('nContent').value='';document.getElementById('nCat').value='personal';
  document.querySelectorAll('#noteModal .cp-opt').forEach(c=>c.classList.toggle('selected',c===document.querySelector('#noteModal .cp-opt')));
  if(id){const n=D.notes?.find(x=>x.id===id);if(n){document.getElementById('noteModalTitle').textContent='Editar Nota';document.getElementById('nId').value=n.id;document.getElementById('nTitle').value=n.title||'';document.getElementById('nContent').value=n.content||'';document.getElementById('nCat').value=n.category;document.querySelectorAll('#noteModal .cp-opt').forEach(c=>c.classList.toggle('selected',c.dataset.c===n.color));document.getElementById('delNoteBtn').style.display='inline-flex';}else document.getElementById('delNoteBtn').style.display='none';}else{document.getElementById('noteModalTitle').textContent='Nueva Nota';document.getElementById('delNoteBtn').style.display='none';}openMod('noteModal');
}

function saveNote(){const t=document.getElementById('nTitle').value.trim(),c=document.getElementById('nContent').value.trim();if(!t&&!c){showToast('Escribe algo','error');return;}const id=document.getElementById('nId').value;const sel=document.querySelector('#noteModal .cp-opt.selected');const data={title:t||'Sin título',content:c,category:document.getElementById('nCat').value,color:sel?sel.dataset.c:'#6c5ce7'};
if(id){data.id=id;data.createdAt=D.notes?.find(x=>x.id===id)?.createdAt||Date.now();}else{data.id=genId();data.createdAt=Date.now();(D.notes||(D.notes=[])).push(data);addAct('📝 <strong>'+data.title+'</strong> creada');showToast('Nota creada','success');}if(id){const i=D.notes?.findIndex(x=>x.id===id);if(i>=0)D.notes[i]={...D.notes[i],...data,updatedAt:Date.now()};}else{addAct('📝 <strong>'+data.title+'</strong> capturada');}saveData();closeMod('noteModal');renderNotes(currentFilter.notes);checkBadges();}

function deleteNote(id){if(id&&confirm('¿Eliminar nota?')){D.notes=D.notes?.filter(n=>n.id!==id);addAct('🗑️ Nota eliminada');saveData();showToast('Nota eliminada');closeMod('noteModal');renderNotes(currentFilter.notes);}}

// ==================== CALENDAR ====================
function renderCalendar(){
  const y=calDate.getFullYear(),m=calDate.getMonth();const n=new Date();
  document.getElementById('calTitle').textContent=new Date(y,m).toLocaleDateString('es-ES',{month:'long',year:'numeric'});
  const g=document.getElementById('calGrid');let h=['L','M','X','J','V','S','D'].map(d=>'<div style="padding:10px;text-align:center;font-size:11px;font-weight:700;color:var(--text-muted);">'+d+'</div>').join('');
  const fd=new Date(y,m,1).getDay(),sd=fd===0?6:fd-1;const dim=new Date(y,m+1,0).getDate();const dpm=new Date(y,m,0).getDate();
  for(let i=sd-1;i>=0;i--)h+=`<div class="mc-day" style="opacity:0.3;">${dpm-i}</div>`;
  for(let d=1;d<=dim;d++){const ds=`${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`;const isToday=n.getFullYear()===y&&n.getMonth()===m&&n.getDate()===d;const hasTask=(D.tasks||[]).filter(t=>t.dueDate===ds).length>0;h+=`<div class="mc-day ${isToday?'today':''} ${hasTask?'active':''}" ondblclick="quickAddDate('${ds}')">${d}</div>`;}
  const rem=((sd+dim)%7===0)?0:7-(sd+dim)%7;for(let i=1;i<=rem;i++)h+=`<div class="mc-day" style="opacity:0.3;">${i}</div>`;
  g.innerHTML=h;
}

function calNav(d){calDate.setMonth(calDate.getMonth()+d);renderCalendar();}
function quickAddDate(ds){document.getElementById('qaDate').value=ds;openQuickAdd();}

// ==================== TRADING ====================
function renderTrading(){
  const trades=D.trades||[];const today=new Date().toDateString();
  const todayTrades=trades.filter(t=>new Date(t.date).toDateString()===today);
  const todayPnL=todayTrades.reduce((s,t)=>s+t.pnl,0);

  document.getElementById('todayPnl').textContent=(todayPnL>=0?'+':'')+'$'+todayPnL.toFixed(2);
  document.getElementById('pnlCard').className='trading-pnl-card '+(todayPnL<0?'negative':'');
  document.getElementById('todayPnl').className='pnl-value '+(todayPnL>=0?'positive':'negative');

  const allTrades=D.trades||[];const wins=allTrades.filter(t=>t.pnl>0);const losses=allTrades.filter(t=>t.pnl<0);const wr=allTrades.length?Math.round(wins.length/allTrades.length*100):0;
  const avgRR=allTrades.length?(wins.reduce((s,t)=>{const r=Math.abs(t.pnl/Math.abs(t.sl||t.size*0.01));return s+r;},0)/wins.length).toFixed(1):'0:1';

  document.getElementById('totalTrades').textContent=allTrades.length;
  document.getElementById('winRate').textContent=wr+'%';document.getElementById('winRate').style.color=wr>=50?'var(--success)':'var(--danger)';
  document.getElementById('avgRR').textContent='1:'+avgRR;
  document.getElementById('totalWins').textContent=wins.length;document.getElementById('totalLosses').textContent=losses.length;

  const best=allTrades.length?Math.max(...allTrades.map(t=>t.pnl)):0;const worst=allTrades.length?Math.min(...allTrades.map(t=>t.pnl)):0;
  document.getElementById('bestTrade').textContent=(best>=0?'+':'')+'$'+best.toFixed(2);document.getElementById('bestTrade').style.color=best>0?'var(--success)':'var(--warning)';

  // Consecutive wins streak
  let cw=0,tradesSorted=[...allTrades].sort((a,b)=>new Date(b.date)-new Date(a.date));
  for(const t of tradesSorted){if(t.pnl>0)cw++;else break;}document.getElementById('consecWins').textContent=cw;

  // Charts
  renderPnLChart();renderWeeklyChart();

  // Trade journal
  const sorted=[...allTrades].sort((a,b)=>new Date(b.date)-new Date(a.date)).slice(0,20);
  document.getElementById('tradeJournal').innerHTML=sorted.length?sorted.map(t=>{
    const cls=t.pnl>0?'win':t.pnl<0?'loss':'be';const sign=t.pnl>=0?'+':'';
    return`<div class="trade-item" onclick="openTradeModal('${t.id}')">
      <div><span style="font-size:24px;">${t.type==='long'?'🟢':'🔴'}</span></div>
      <div class="trade-details"><div class="td-pair">${escape(t.pair)}</div><div class="td-meta">${fmtD(t.date)} • ${escape(t.strat||'Sin estrategia')} • ${eLabel(t.emo)}</div></div>
      <div class="trade-pnl-badge ${cls}">${sign}$${t.pnl.toFixed(2)}</div>
    </div>`;}).join(''):'<div style="text-align:center;padding:30px;color:var(--text-muted);font-size:14px;">Registra tu primer trade 💹</div>';

  document.getElementById('tradesCount').textContent=allTrades.length;
}

function renderPnLChart(){
  const c=document.getElementById('pnlChart');if(!c)return;const ctx=c.getContext('2d');
  const W=c.width=c.offsetWidth*2,H=c.height=400;
  const trades=[...D.trades||[]].sort((a,b)=>new Date(a.date)-new Date(b.date));

  // Build cumulative PnL
  let cumPnL=[];let sum=0;trades.forEach(t=>{sum+=t.pnl;cumPnL.push({d:new Date(t.date),pnl:sum});});
  if(!cumPnL.length)cumPnL=[{d:new Date(),pnl:0}];

  const pad={t:20,r:20,b:40,l:60};const cw=W-pad.l-pad.r;const ch=H-pad.t-pad.b;
  const allVals=cumPnL.map(p=>p.pnl);const mn=Math.min(...allVals,0);const mx=Math.max(...allVals,0);const rng=mx-mn||1;

  ctx.clearRect(0,0,W,H);

  // Zero line
  const zeroY=pad.t+ch*(1-(0-mn)/rng);
  ctx.strokeStyle='rgba(255,255,255,0.06)';ctx.lineWidth=1;ctx.setLineDash([4,4]);ctx.beginPath();ctx.moveTo(pad.l,zeroY);ctx.lineTo(W-pad.r,zeroY);ctx.stroke();ctx.setLineDash([]);

  // Grid
  for(let i=0;i<=4;i++){const y=pad.t+ch*(i/4);ctx.strokeStyle='rgba(255,255,255,0.03)';ctx.beginPath();ctx.moveTo(pad.l,y);ctx.lineTo(W-pad.r,y);ctx.stroke();}

  // Gradient fill
  const grad=ctx.createLinearGradient(0,pad.t,0,H-pad.b);const positive=cumPnL[cumPnL.length-1].pnl>=0;
  if(positive){grad.addColorStop(0,'rgba(0,214,143,0.3)');grad.addColorStop(1,'rgba(0,214,143,0)');}else{grad.addColorStop(0,'rgba(255,71,87,0.3)');grad.addColorStop(1,'rgba(255,71,87,0)');}

  // Line
  ctx.beginPath();ctx.strokeStyle=positive?'#00d68f':'#ff4757';ctx.lineWidth=3;
  cumPnL.forEach((p,i)=>{const x=pad.l+(i/(cumPnL.length-1||1))*cw;const y=pad.t+ch*(1-(p.pnl-mn)/rng);i===0?ctx.moveTo(x,y):ctx.lineTo(x,y);});
  ctx.stroke();

  // Fill
  const lastX=pad.l+cw;const path=new Path2D();path.moveTo(pad.l,zeroY);cumPnL.forEach((p,i)=>{const x=pad.l+(i/(cumPnL.length-1||1))*cw;const y=pad.t+ch*(1-(p.pnl-mn)/rng);path.lineTo(x,y);});path.lineTo(lastX,zeroY);path.closePath();ctx.fillStyle=grad;ctx.fill(path);

  // Dots
  cumPnL.forEach((p,i)=>{const x=pad.l+(i/(cumPnL.length-1||1))*cw;const y=pad.t+ch*(1-(p.pnl-mn)/rng);ctx.beginPath();ctx.arc(x,y,4,0,Math.PI*2);ctx.fillStyle=positive?'#00d68f':'#ff4757';ctx.fill();});

  // Y labels
  ctx.fillStyle='#55557a';ctx.font='18px system-ui';ctx.textAlign='right';
  for(let i=0;i<=4;i++){const val=mn+rng*(1-i/4);ctx.fillText('$'+val.toFixed(0),pad.l-8,pad.t+ch*(i/4)+6);}

  // X labels (dates)
  ctx.textAlign='center';if(cumPnL.length>1){const step=Math.max(1,Math.floor(cumPnL.length/5));cumPnL.forEach((p,i)=>{if(i%step===0||i===cumPnL.length-1){const x=pad.l+(i/(cumPnL.length-1))*cw;ctx.fillStyle='#55557a';ctx.fillText(fmtD(p.d),x,H-pad.b+24);}});}
}

function renderWeeklyChart(){
  const c=document.getElementById('weeklyChart');if(!c)return;const ctx=c.getContext('2d');
  const W=c.width=c.offsetWidth*2,H=c.height=320;
  const days=[];for(let i=6;i>=0;i--){const d=new Date();d.setDate(d.getDate()-i);days.push({d:d.toLocaleDateString('es-ES',{weekday:'short'}),date:d.toDateString(),p:0});}
  (D.trades||[]).forEach(t=>{const td=new Date(t.date).toDateString();days.forEach(ds=>{if(ds.date===td)ds.p+=t.pnl;});});

  const pad={t:20,r:20,b:40,l:60};const cw=W-pad.l-pad.r;const ch=H-pad.t-pad.b;
  const mn=Math.min(...days.map(d=>d.p),0);const mx=Math.max(...days.map(d=>d.p),1);const rng=mx-mn||1;

  ctx.clearRect(0,0,W,H);
  // Zero line
  const zeroY=pad.t+ch*(1-(0-mn)/rng);ctx.strokeStyle='rgba(255,255,255,0.06)';ctx.lineWidth=1;ctx.setLineDash([4,4]);ctx.beginPath();ctx.moveTo(pad.l,zeroY);ctx.lineTo(W-pad.r,zeroY);ctx.stroke();ctx.setLineDash([]);

  const bw=cw/days.length*0.5;
  days.forEach((d,i)=>{
    const x=pad.l+i*(cw/days.length)+cw/days.length*0.25;const bh=Math.abs(d.p)/rng*ch*0.8;const y=d.p>=0?zeroY-bh:zeroY;
    const grad=ctx.createLinearGradient(x,y,x,zeroY);grad.addColorStop(0,d.p>=0?'rgba(0,214,143,0.8)':'rgba(255,71,87,0.8)');grad.addColorStop(1,d.p>=0?'rgba(0,214,143,0.1)':'rgba(255,71,87,0.1)');
    ctx.fillStyle=grad;roundRect(ctx,x,y,bw,bh||4,6);ctx.fill();

    ctx.fillStyle='#f0f0f8';ctx.font='bold 22px system-ui';ctx.textAlign='center';
    ctx.fillText((d.p>=0?'+':'')+'$'+Math.abs(d.p).toFixed(0),x+bw/2,y-(d.p>=0?16:4));
    ctx.fillStyle='#55557a';ctx.font='20px system-ui';ctx.fillText(d.d,x+bw/2,H-pad.b+28);
  });
}

function openTradeModal(id){
  document.getElementById('trId').value=id||'';document.getElementById('trPair').value='';document.getElementById('trType').value='long';
  document.getElementById('trEntry').value='';document.getElementById('trExit').value='';document.getElementById('trSize').value='';
  document.getElementById('trPnl').value='';document.getElementById('trSL').value='';document.getElementById('trTP').value='';
  document.getElementById('trStrat').value='';document.getElementById('trEmo').value='calm';document.getElementById('trNotes').value='';
  document.getElementById('delTradeBtn').style.display='none';

  if(id){const t=(D.trades||[]).find(x=>x.id===id);if(t){document.getElementById('trId').value=t.id;document.getElementById('trPair').value=t.pair;document.getElementById('trType').value=t.type;document.getElementById('trEntry').value=t.entry;document.getElementById('trExit').value=t.exit;document.getElementById('trSize').value=t.size;document.getElementById('trPnl').value=t.pnl;document.getElementById('trSL').value=t.sl||'';document.getElementById('trTP').value=t.tp||'';document.getElementById('trStrat').value=t.strat||'';document.getElementById('trEmo').value=t.emo||'calm';document.getElementById('trNotes').value=t.notes||'';document.getElementById('delTradeBtn').style.display='inline-flex';}}openMod('tradeModal');
}

function saveTrade(){
  const pair=document.getElementById('trPair').value.trim();if(!pair){showToast('Ingresa el par','error');return;}
  const id=document.getElementById('trId').value;let pnl=parseFloat(document.getElementById('trPnl').value)||0;
  // Auto-calculate PnL if not provided
  if(!document.getElementById('trPnl').value&&document.getElementById('trEntry').value&&document.getElementById('trExit').value){const entry=parseFloat(document.getElementById('trEntry').value);const exit=parseFloat(document.getElementById('trExit').value);const size=parseFloat(document.getElementById('trSize').value)||1;const type=document.getElementById('trType').value;pnl=(type==='long'?exit-entry:entry-exit)*size;}

  const data={pair,type:document.getElementById('trType').value,entry:parseFloat(document.getElementById('trEntry').value)||0,exit:parseFloat(document.getElementById('trExit').value)||0,size:parseFloat(document.getElementById('trSize').value)||0,pnl,strat:document.getElementById('trStrat').value,sl:parseFloat(document.getElementById('trSL').value),tp:parseFloat(document.getElementById('trTP').value),emo:document.getElementById('trEmo').value,notes:document.getElementById('trNotes').value};

  if(id){const i=(D.trades||[]).findIndex(t=>t.id===id);if(i>=0)D.trades[i]={...D.trades[i],...data,updatedAt:Date.now()};addAct('📊 Trade editado: <strong>'+pair+'</strong> (PnL: $'+pnl.toFixed(2)+')');showToast('Trade actualizado','info');}
  else{data.id=genId();data.date=new Date().toISOString();data.createdAt=Date.now();(D.trades||(D.trades=[])).push(data);addAct('📊 Nuevo trade: <strong>'+pair+'</strong> (PnL: $'+pnl.toFixed(2)+')');showToast('Trade registrado',pnl>=0?'success':'error');if(pnl>=0){addXP(5);D.totalCompleted++;saveData();}}
  saveData();closeMod('tradeModal');renderTrading();}

function deleteTrade(id){if(id&&confirm('¿Eliminar este trade?')){D.trades=D.trades?.filter(t=>t.id!==id);addAct('🗑️ Trade eliminado');saveData();showToast('Trade eliminado');closeMod('tradeModal');renderTrading();}}

// ==================== CRYPTO NEWS ====================
const NEWS_TEMPLATES=[
  {s:'CoinDesk',titles:['Bitcoin supera resistencia clave con volumen institucional','Ethereum prepare actualización importante para redes Layer 2','Regulación crypto: nuevos marcos regulatorios en la UE afectan mercados']},
  {s:'Cointelegraph',titles:['Mayor adopción de DeFi entre instituciones financieras tradicionales','Solana registra nuevo máximo de transacciones diarias','Tokenización de activos reales alcanza $10B en volumen']},
  {s:'The Block',titles:['ETF de Bitcoin alcanza récord de entradas semanales','MicroStrategy adquiere más BTC por valor desconocido','Mercados crypto reaccionan a datos de inflación de EE.UU.']},
  {s:'CryptoSlate',titles:['Avalancha de nuevos proyectos DeFi en Q1','Whale alert: grandes movimientos de Bitcoin detectados','Análisis on-chain sugiere fase acumulativa para altcoins']},
  {s:'Decrypt',titles:['Web3 gaming atrae inversión millonaria de venture capital','NFT market muestra señales de recuperación sostenida','Layer 2 solutions compiten por liderar escalabilidad']},
  {s:'BeInCrypto',titles:['Reserva Federal señala posibles cambios en política monetaria','Banco Central Europeo avanza con el digital euro','Commodity Futures Trading Commission revisa criterios para crypto']}
];

const MARKET_COINS=[
  {name:'Bitcoin',symbol:'BTC',price:'$67,234',change:'+2.4%',up:true},
  {name:'Ethereum',symbol:'ETH',price:'$3,456',change:'+1.8%',up:true},
  {name:'Solana',symbol:'SOL',price:'$178.90',change:'-0.5%',up:false},
  {name:'BNB',symbol:'BNB',price:'$612.30',change:'+0.9%',up:true},
  {name:'XRP',symbol:'XRP',price:'$0.6234',change:'+3.1%',up:true},
  {name:'ADA',symbol:'ADA',price:'$0.4521',change:'-1.2%',up:false}
];

function renderNews(){
  // Market overview
  document.getElementById('marketOverview').innerHTML=MARKET_COINS.map(c=>`
    <div class="ts-card" style="cursor:pointer;"><div style="font-size:10px;color:var(--text-muted);margin-bottom:4px;">${c.symbol}</div><div class="ts-val" style="font-size:16px;">${c.price}</div><div class="ts-lbl" style="color:${c.up?'var(--success)':'var(--danger)'};">${c.change}</div></div>`).join('');

  // News
  const news=NEWS_TEMPLATES.sort(()=>Math.random()-0.5).slice(0,6);
  document.getElementById('newsGrid').innerHTML=news.map(n=>{
    return n.titles.map(t=>`<div class="news-card"><div class="nc-source">${n.s}</div><div class="nc-title">${t}</div><div class="nc-time">Hace ${Math.floor(Math.random()*3)+1}h • Lectura 3 min</div></div>`);
  }).flat();
}

function loadCryptoNews(){renderNews();showToast('Noticias actualizadas','info');}

// ==================== ACHIEVEMENTS / PROFILE ====================
function renderAchievements(){
  updateXPUI();renderBadges();
  document.getElementById('profileLvl').textContent=D.level;
  document.getElementById('profileXP').textContent=D.xp;
  document.getElementById('totalCompleted').textContent=D.totalCompleted||0;
}

// ==================== QUICK ADD ====================
function openQuickAdd(){
  document.getElementById('qaText').value='';document.getElementById('qaType').value='task';
  document.getElementById('qaPriority').value='medium';document.getElementById('qaDate').value='';populateSelects();openMod('quickModal');setTimeout(()=>document.getElementById('qaText').focus(),200);}

function submitQuick(){const t=document.getElementById('qaText').value.trim();if(!t){showToast('Escribe algo','error');return;}
  const type=document.getElementById('qaType').value,prio=document.getElementById('qaPriority').value;
  if(type==='task'){(D.tasks||(D.tasks=[])).push({id:genId(),title:t,description:'',priority:prio,status:'pending',startDate:'',dueDate:document.getElementById('qaDate').value,projectId:document.getElementById('qaProject').value,category:'',subtasks:[],createdAt:Date.now()});addAct('⚡ Captura rápida: <strong>"'+t+'"');showToast('Tarea capturada ⚡','success');}
  else{(D.notes||(D.notes=[])).push({id:genId(),title:t.substring(0,50),content:t,category:'ideas',color:'#6c5ce7',createdAt:Date.now()});addAct('⚡ Nota rápida capturada');showToast('Nota creada ⚡','success');}
  saveData();closeMod('quickModal');renderPage(curPage);checkBadges();}

// ==================== MODAL HELPERS ====================
function openTaskModal(id){
  document.getElementById('tId').value=id||'';document.getElementById('tTitle').value='';document.getElementById('tDesc').value='';
  document.getElementById('tPrio').value='medium';document.getElementById('tStatus').value='pending';
  document.getElementById('tStart').value='';document.getElementById('tDue').value='';document.getElementById('tProj').value='';document.getElementById('tCat').value='';
  document.getElementById('delTaskBtn').style.display=id?'inline-flex':'none';
  if(id){const t=(D.tasks||[]).find(x=>x.id===id);if(t){document.getElementById('taskModalTitle').textContent='Editar Tarea';document.getElementById('tId').value=t.id;document.getElementById('tTitle').value=t.title;document.getElementById('tDesc').value=t.description||'';document.getElementById('tPrio').value=t.priority;document.getElementById('tStatus').value=t.status;document.getElementById('tStart').value=t.startDate||'';document.getElementById('tDue').value=t.dueDate||'';document.getElementById('tProj').value=t.projectId||'';document.getElementById('tCat').value=t.category||'';}}else document.getElementById('taskModalTitle').textContent='Nueva Tarea';populateSelects();openMod('taskModal');}

function saveTask(){const t=document.getElementById('tTitle').value.trim();if(!t){showToast('Título requerido','error');return;}const id=document.getElementById('tId').value;const data={title:t,description:document.getElementById('tDesc').value.trim(),priority:document.getElementById('tPrio').value,status:document.getElementById('tStatus').value,startDate:document.getElementById('tStart').value,dueDate:document.getElementById('tDue').value,projectId:document.getElementById('tProj').value,category:document.getElementById('tCat').value};
if(id){const i=(D.tasks||[]).findIndex(x=>x.id===id);if(i>=0)D.tasks[i]={...D.tasks[i],...data,updatedAt:Date.now()};addAct('✏️ Tarea editada: <strong>'+t+'</strong>');showToast('Tarea actualizada');}
else{data.id=genId();data.createdAt=Date.now();(D.tasks||(D.tasks=[])).push(data);addAct('✅ Tarea creada: <strong>'+t+'</strong>');showToast('Tarea creada','success');}saveData();closeMod('taskModal');renderPage(curPage);}

function deleteTask(id){if(!id)return;if(confirm('¿Eliminar esta tarea?')){D.tasks=D.tasks?.filter(t=>t.id!==id);addAct('🗑️ Tarea eliminada');saveData();showToast('Tarea eliminada');closeMod('taskModal');renderPage(curPage);}}

// ==================== SEARCH ====================
function handleSearch(q){if(!q.trim())return;const r=[];(D.tasks||[]).forEach(t=>{if(t.title.toLowerCase().includes(q.toLowerCase()))r.push({t:'✅ Tarea',n:t.title,a:()=>openTaskModal(t.id)});});(D.projects||[]).forEach(p=>{if(p.name.toLowerCase().includes(q.toLowerCase()))r.push({t:'📁 Proyecto',n:p.name,a:()=>openProjectModal(p.id)});});(D.notes||[]).forEach(n=>{if((n.title||'').toLowerCase().includes(q.toLowerCase()))r.push({t:'📝 Nota',n:n.title,a:()=>openNoteModal(n.id)});});
const el=document.getElementById('globalSearch');el.setAttribute('data-results',JSON.stringify(r));}

// ==================== UTILITIES ====================
function escape(s){const d=document.createElement('div');d.textContent=s||'';return d.innerHTML;}
function fmtD(d){if(!d)return '';try{return new Date(d).toLocaleDateString('es-ES',{day:'numeric',month:'short'});}catch(e){return '';} }
function pLabel(p){return{low:'Baja',medium:'Media',high:'Alta',urgent:'Urgente'}[p]||p;}
function prioTag(p){return{low:'green',medium:'yellow',high:'orange',urgent:'purple'}[p]||'gray';}
function sLabel(s){return{active:'🟢 Activo',paused:'🟡 Pausado',completed:'✅ Completado'}[s]||s;}
function eLabel(e){const m={calm:'😌 Calma',confident:'💪 Confianza',fomo:'😰 FOMO',revenge:'🔥 Revenge',disciplined:'🎯 Disciplinado'};return m[e]||e;}
function getProjName(id){const p=(D.projects||[]).find(x=>x.id===id);return p?escape(p.name):null;}

function populateSelects(){['qaProject','tProj'].forEach(id=>{const sel=document.getElementById(id);if(!sel)return;sel.innerHTML='<option value="">-- Sin proyecto --</option>';(D.projects||[]).filter(p=>p.status!=='completed').forEach(p=>{sel.innerHTML+=`<option value="${p.id}">${escape(p.name)}</option>`;});});}

function updateBadges(){document.getElementById('navTaskBadge').textContent=(D.tasks||[]).filter(t=>t.status!=='completed').length;document.getElementById('navNoteBadge').textContent=D.notes?.length||0;}

// ==================== CHECK BADGES ====================
function checkBadges(){if(D.totalCompleted>=1)checkBadge('first_task');if(getStreak()>=3)checkBadge('streak_3');}
function checkBadge(id){/* auto-unlock */}

// ==================== INIT ====================
function init(){
  // Default goals if empty
  if(!D.dailyGoals||!D.dailyGoals.length){D.dailyGoals=['Completar al menos 5 tareas hoy','Crear una nota de ideas','Registrar un trade en el trading desk','Revisar proyecto activo'];}

  updateBadges();updateXPUI();updateStreakUI();renderDashboard();
  document.getElementById('fabBtn').style.display='block';
  window.addEventListener('resize',()=>{if(curPage==='tasks')renderTaskChart();});
}

// Swipe down to close sidebar on mobile
let touchStartY=0;document.addEventListener('touchstart',e=>{touchStartY=e.touches[0].clientY;},{passive:true});
document.addEventListener('touchmove',e=>{},{passive:true});

init();
</script>
</body>
</html>
